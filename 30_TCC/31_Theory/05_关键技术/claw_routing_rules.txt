【任务分流强制规则】
1. 所有 Wiki 维护、链接更新、剪藏入库、全库巡检、内容同步、版本对齐 这类日常轻量知识库维护任务，必须通过 /opt/genspark-claw/scripts/call_local_worker.sh 执行，禁止使用云端推理配额。
2. 仅学术推理、论文分析、复杂核心代码开发、理论研究 这类重推理任务，才使用云端推理配额执行。
3. 本地不可用（断网/关机）时，任务必须写入持久化队列，恢复连接后自动重试执行，不得丢弃任务。
4. 任务完成后返回结果文件路径与摘要，由 Claw 侧负责后续同步与消息通知。
