---
title: "FA 提速近 2 倍！从 Tile 语言到空间架构：TileLoom 实现自动数据流规划破解空间加速器编程难题，性能追平厂商手工库"
source: "https://mp.weixin.qq.com/s/tL9ahY89BVZ2EVSyp_pfow"
author:
  - "[[NUS, ASU, Google]]"
published:
created: 2026-07-06
description: "空间数据流加速器空有极致算力却长期深陷可编程性困境？TileLoom 基于 MLIR 实现自动数据流规划，将 Triton 等 tile 语言映射到空间架构，在两代 Tenstorrent 平台 FlashAttention 显著提速近 2 倍，性能追平厂商手工优化库。"
tags:
  - "clippings"
provenance: external
---
NUS, ASU, Google NeuralTalk *2026年7月6日 08:49*

**投稿/寻求报道/文章纠错：公众号后台 -> 联系我们**

关键词： **空间数据流加速器** 、 ***MLIR 编译框架*** 、 **tile 编程语言** 、自动数据流规划、Tenstorrent

,16分钟

![图片](https://mmbiz.qpic.cn/sz_mmbiz_png/GxIgp4icchHibjBuu7UicB1Hwh3PEia1BUzLia5GwdTWDGOFoUTuibGibianKjDfR0QFoPiasfwfFDcfC1jNibtAcyAom2u7oCRoSmLvLe6lKKpTGuicQQ/640?wx_fmt=png&from=appmsg&watermark=1&tp=webp&wxfrom=5&wx_lazy=1#imgIndex=0)

- **TileLoom: Automatic Dataflow Planning for Tile-Based Languages on Spatial Dataflow Accelerators**
- 论文：https://arxiv.org/pdf/2512.22168v2
- 代码：https://github.com/ecolab-nus/loom-dataflow

,16分钟

**相关推荐**

- **[GEMM/FA 平均性能超厂商库 1.03×/1.91×！面向空间数据流架构的端到端 Tile-Based 编译器 TL](https://mp.weixin.qq.com/s?__biz=MjM5NDczOTA4NQ==&mid=2447896903&idx=1&sn=78559ef90da4c8638475faac4265f65a&scene=21#wechat_redirect)**
- **[碾压GH200注意力实现：FlatAttention在Tile数据流加速器上，计算场景利用率达86%，访存场景带宽利用率达 78%，注意力内核平均加速 1.9倍](https://mp.weixin.qq.com/s?__biz=MjM5NDczOTA4NQ==&mid=2447899603&idx=1&sn=59da0ff7b8208a308553b463fc36a817&scene=21#wechat_redirect)**
- **[解锁下一代 AI 加速器性能潜力！普渡、Meta提出 AI 加速器复合操作数据流和显式集体通信建模方案 COMET](https://mp.weixin.qq.com/s?__biz=MjM5NDczOTA4NQ==&mid=2447896642&idx=1&sn=ed9b64a8a02e6db7a61a486e6fe64d65&scene=21#wechat_redirect)**

> 论文中提到， **尽管空间数据流加速器在性能、能效与成本效率上具备巨大潜力，但有限的可编程性仍是其大规模普及的核心障碍** 。这一判断精准点出了 ***当前 AI 专用芯片领域的普遍困境：硬件架构已经走在了软件的前面，大量算力因为缺乏高效的编译工具而无法被普通开发者利用。***

TileLoom 这项工作的核心价值，就是为空间数据流加速器提供了一套通用的自动化编译方案。它基于 MLIR 构建了端到端的编译栈，能够将 Triton 这类面向 tile 的高级编程语言，自动映射到空间数据流架构上，自动完成时空映射、数据复用规划与通信调度，不再需要专家手工为每个算子编写硬件专用的优化代码。

![图 2：TileLoom 整体框架总览
。该图完整展示三层端到端编译流水线，串联 tile 级 DSL 输入到硬件可执行程序全链路，分为前端、数据流规划、后端代码生成三大核心模块。前端负责 Triton/Helion 内核分块形状自动调优，输出硬件无关的标准化 MLIR；核心数据流规划模块依托自定义 df 硬件描述方言，完成时空分块映射、跨核数据复用规划，再通过性能模型筛选最优候选映射；后端对接 Tenstorrent 底层 TT-Metalium API 生成硬件指令。框架创新引入 top-k 静态筛选+硬件二次调优的双层候选选择机制，弥补硬件建模存在的精度误差。三层解耦架构实现强硬件无关性，一套编译流程可适配二维网格、一维环形等多种空间数据流加速器，解决厂商专属库绑定硬件、难以快速拓展新型算子的行业痛点。](https://mmbiz.qpic.cn/mmbiz_png/GxIgp4icchH9g4ux7tefvo2WBFxMzGubQsv1wpuIUe7kQKagdicmH8Waxb20nml7CRZQ6Mny1jiaU0sZIsZ3vnicoA1kn2ep8zZK2gicDiaygVKh8/640?wx_fmt=png&from=appmsg&watermark=1&tp=webp&wxfrom=5&wx_lazy=1#imgIndex=1)

图 2：TileLoom 整体框架总览 。该图完整展示三层端到端编译流水线，串联 tile 级 DSL 输入到硬件可执行程序全链路，分为前端、数据流规划、后端代码生成三大核心模块。前端负责 Triton/Helion 内核分块形状自动调优，输出硬件无关的标准化 MLIR；核心数据流规划模块依托自定义 df 硬件描述方言，完成时空分块映射、跨核数据复用规划，再通过性能模型筛选最优候选映射；后端对接 Tenstorrent 底层 TT-Metalium API 生成硬件指令。框架创新引入 top-k 静态筛选+硬件二次调优的双层候选选择机制，弥补硬件建模存在的精度误差。三层解耦架构实现强硬件无关性，一套编译流程可适配二维网格、一维环形等多种空间数据流加速器，解决厂商专属库绑定硬件、难以快速拓展新型算子的行业痛点。

在两代 Tenstorrent 硬件上的实验结果显示，TileLoom 编译生成的代码， **在 FlashAttention 算子上实现了接近 2 倍的性能提升，在 GEMM 这类已经被厂商深度优化的算子上也能达到持平甚至反超的水平** ，对于厂商没有提供融合实现的算子，更是能带来十几倍到几十倍的性能飞跃。这一成果不仅证明了自动化数据流规划的可行性，也为空间数据流架构的软件生态建设提供了新的思路。

![表 2：TileLoom 相对 TTNN 在矩阵乘法、FlashAttention、Flash 解码、Mamba 分块扫描线性注意力算子上的性能倍数
。FlashAttention 具备充足跨核空间复用，两款硬件均接近 2 倍加速；Flash Decode 并行维度稀缺，仅达到厂商库 84%-87%性能；标准 GEMM 在 Wormhole 算力受限场景略弱，Blackhole 带宽瓶颈场景反超至 1.10 倍；Mamba 因厂商无原生融合算子，实现 16~27 倍巨大加速。该表清晰区分四类典型场景：复用充足的注意力算子、并行受限的解码算子、成熟高度优化的矩阵算子、无原生融合的新型线性算子，完整论证 TileLoom 对新型算子、带宽受限新一代硬件的独特价值，同时客观承认成熟算子场景下与手工调优库存在小幅性能差距。](https://mmbiz.qpic.cn/mmbiz_png/GxIgp4icchHicvesC8zo40efjLkjgJHrzMDqr4aP7PBW5fSWrYO7w6Z9yvR0OwkL62j28aH6GzvNibtuGQNaKO9F2r06WbMHbDcwBicAefvbuQ4/640?wx_fmt=png&from=appmsg&watermark=1#imgIndex=2)

表 2：TileLoom 相对 TTNN 在矩阵乘法、FlashAttention、Flash 解码、Mamba 分块扫描线性注意力算子上的性能倍数 。FlashAttention 具备充足跨核空间复用，两款硬件均接近 2 倍加速；Flash Decode 并行维度稀缺，仅达到厂商库 84%-87%性能；标准 GEMM 在 Wormhole 算力受限场景略弱，Blackhole 带宽瓶颈场景反超至 1.10 倍；Mamba 因厂商无原生融合算子，实现 16~27 倍巨大加速。该表清晰区分四类典型场景：复用充足的注意力算子、并行受限的解码算子、成熟高度优化的矩阵算子、无原生融合的新型线性算子，完整论证 TileLoom 对新型算子、带宽受限新一代硬件的独特价值，同时客观承认成熟算子场景下与手工调优库存在小幅性能差距。

接下来我们将从背景痛点、架构设计、核心技术、实验验证等多个维度，对这项工作进行全面深入的解读。

## 本文目录

- 一、空间数据流架构的算力红利与可编程性鸿沟
- 1.1 内存墙：传统架构无法逾越的算力天花板
	- 1.2 空间数据流：分布式算力与存储的破局之路
	- 1.3 编程错位：GPU 时代的抽象不再适配空间架构
- 二、TileLoom 整体设计：把硬件调度权完全交给编译器
- 2.1 核心洞察：空间架构的可预测性让静态编译成为可能
	- 2.2 三层编译流水线：从 tile DSL 到硬件可执行文件
	- 2.3 两阶段选择策略：模型预筛与硬件实测的平衡
- 三、时空映射：将逻辑 tile 网格铺展到物理核心阵列
- 3.1 统一中间表示：用仿射循环统一所有 tile 计算
	- 3.2 映射设计空间：三个耦合决策定义全部数据流向
	- 3.3 分块式映射原则：保留数据局部性是核心前提
- 四、数据复用规划：榨干片上网络与分布式缓存的每一分价值
- 4.1 仿射访问分析：自动识别时空复用机会
	- 4.2 空间复用：用片上广播替代重复全局访存
	- 4.3 时间复用：循环提升与缓冲区容量的权衡
	- 4.4 时空复用正交组合：最大化数据的复用次数
- 五、统一硬件抽象：用一套 IR 描述所有空间数据流架构
- 5.1 为什么需要可复用的硬件表示
	- 5.2 df 方言的三层抽象栈：从拓扑到计算单元
	- 5.3 拓扑无关性：从 2D Mesh 到 1D 环形的平滑适配
- 六、性能建模：编译期精准预测映射的实际执行耗时
- 6.1 分层成本聚合：从内层循环向外逐层估算
	- 6.2 计算访存重叠：双缓冲流水线的时序模型
	- 6.3 网络拥塞建模：并发传输下的有效带宽折算
	- 6.4 定位与精度：排序正确比周期精准更重要
- 七、实验验证：两代 Tenstorrent 硬件上的真实表现
- 7.1 测试平台：Wormhole 与 Blackhole 的硬件规格
	- 7.2 核心算子性能：四大典型算子的横向对比
	- 7.3 消融实验：空间与时间复用的真实收益
	- 7.4 模型精度与 top-k 调优的投入产出比
- 八、定位与对比：TileLoom 在编译技术版图中的位置
- 8.1 与硬件协同设计工具的差异
	- 8.2 与空间架构编译映射工作的差异
- 九、总结与展望
- 9.1 核心贡献与产业价值
	- 9.2 局限与未来方向
![图片](https://mmbiz.qpic.cn/sz_mmbiz_png/GxIgp4icchHibRNMajSmYln9pBe6y5RdGJwo19l7XKnSrg7KhyhbibwWwIh5iarSfNibNqeDKkvK6IwsKgLW6iaMu1UqiadfvwldMJWQOf1j6jFRp8/640?wx_fmt=png&from=appmsg&watermark=1#imgIndex=3)

**交流加群请在 NeuralTalk 公众号后台回复：加群**

## 一、空间数据流架构的算力红利与可编程性鸿沟

> 过去十年，AI 模型的规模与算力需求呈指数级增长，而传统冯·诺依曼架构的算力增长速度，已经远远跟不上模型扩张的脚步。

制约算力释放的核心瓶颈早已不是计算单元本身，而是内存带宽与数据搬运的能耗，这一被称为内存墙的问题，正在成为大模型时代的核心掣肘。

### 1.1 内存墙：传统架构无法逾越的算力天花板

在传统的 CPU 与 GPU 架构中，计算单元和存储单元是分离的，数据需要从 DRAM 搬到缓存，再搬到寄存器，才能被计算单元使用。随着工艺制程的进步，片上可以集成的算术单元越来越多，但片外 DRAM 的带宽增长却非常缓慢，每一次 DRAM 访问的能耗也远高于一次算术运算。

对于深度学习这类数据密集型工作负载来说，大部分时间里计算单元都在等待数据，无法跑满峰值算力。业界常用 Roofline 模型来描述这一现象： **当计算任务的算术强度低于某个阈值时，性能完全由内存带宽决定，再强的计算单元也发挥不了作用。 *而大模型中的注意力、归一化、逐元素激活等大量算子，恰恰都属于带宽密集型，这就导致传统 GPU 的算力利用率长期处于较低水平。***

更严峻的是， **这一趋势还在不断加剧。先进工艺下片上 SRAM 的面积与功耗成本越来越高，堆大缓存的性价比持续下降，而 HBM 显存的带宽增长也逐渐接近物理极限** 。如果继续沿着传统冯·诺依曼的路线走下去，内存墙会成为越来越难以逾越的障碍。

### 1.2 空间数据流：分布式算力与存储的破局之路

> 正是在这样的背景下，空间数据流架构开始进入产业界的视野。包括 Tenstorrent、Cerebras、Graphcore、Groq 在内的多家公司，都推出了基于这一理念的 AI 加速芯片，国内也有不少团队在跟进相关方向。

空间数据流架构的核心思路，是打破集中式的存储与调度模式，将计算单元、本地高速缓存和路由单元打包成一个个独立的核心，再通过高速片上网络把这些核心连成二维或者一维的阵列。数据不再需要统一回到全局缓存或者显存，而是可以 **直接在核心之间传递，计算在哪里发生，数据就流动到哪里。**

![图 1：建模 Tenstorrent-Wormhole 系统的二维网格空间数据流加速器架构示例。
该图是全文硬件建模、实验评估的基准硬件原型，Wormhole 采用 8×8 二维网格核心阵列，每个核心搭载 1.5MB 本地 L1 暂存存储器、矩阵与向量运算单元，依靠分组交换片上 NoC 互联，搭配 4 通道 3GB DRAM 通道。该架构无统一全局共享缓存，全部跨核数据交互依赖显式网络转发，单核心本地带宽累加峰值达 24.5TB/s，大幅超越 NVIDIA H100 L2 带宽，能有效缓解带宽绑定算子的内存瓶颈。](https://mmbiz.qpic.cn/sz_mmbiz_png/GxIgp4icchH9uLXMHJwEHC9XTb0nKNrWl7QoiawKSGR6iaO1Epfib05G2ibpNoGXuz6sOPuDc8pib5DkIgGGBgEH3I7oibhkl7HoMTjO3fKD0cqCmk/640?wx_fmt=png&from=appmsg&watermark=1#imgIndex=4)

图 1：建模 Tenstorrent-Wormhole 系统的二维网格空间数据流加速器架构示例。 该图是全文硬件建模、实验评估的基准硬件原型，Wormhole 采用 8×8 二维网格核心阵列，每个核心搭载 1.5MB 本地 L1 暂存存储器、矩阵与向量运算单元，依靠分组交换片上 NoC 互联，搭配 4 通道 3GB DRAM 通道。该架构无统一全局共享缓存，全部跨核数据交互依赖显式网络转发，单核心本地带宽累加峰值达 24.5TB/s，大幅超越 NVIDIA H100 L2 带宽，能有效缓解带宽绑定算子的内存瓶颈。

这种架构的优势非常明显：

- 首先，片上短距离连线的延迟和能耗远低于访问全局缓存或者片外显存， **数据直接在核心间传递可以大幅降低搬运成本；**
- 其次， **每个核心的本地存储可以并行访问** ，聚合带宽远高于传统的共享缓存，能够有效缓解带宽瓶颈；
- 最后， **硬件结构相对简单** ，不需要复杂的缓存一致性逻辑和动态调度器，更容易做大规模扩展，能效比也更高。

以 Tenstorrent 的 Wormhole 芯片为例，64 个核心组成 8×8 的网格，每个核心有 1.5MB 的本地 SPM，聚合峰值带宽达到 24.5TB/s，而 NVIDIA H100 的 L2 缓存总带宽只有 6TB/s。对于带宽密集型算子来说，这种架构天然具备性能优势。

### 1.3 编程错位：GPU 时代的抽象不再适配空间架构

> **空间架构的硬件优势很突出，但软件可编程性的问题也同样突出** 。在传统 GPU 上，程序员只需要按照网格-线程块-线程的模型编写代码，硬件会自动完成线程块调度、缓存管理和数据复用，编译器只需要关注单个线程块内的优化。

**但在空间数据流架构上，没有硬件调度器来自动分配任务，也没有共享缓存来自动捕获数据复用** 。所有的核心分配、数据布局、通信调度都需要软件来决定， ***程序员不仅要考虑单个核心内怎么算，还要考虑 tile 怎么分布到各个核心、数据怎么在核心间传递、怎么调度才能避免网络拥塞和负载不均。***

这就导致了非常高的编程门槛。普通开发者根本没有能力写出高效的空间架构代码，大家只能依赖厂商提供的手工优化算子库。这些库由资深专家花费大量时间调优，只覆盖矩阵乘法、卷积、注意力等少数常用算子，一旦用户需要自定义算子或者新的模型结构，就很难获得理想的性能。

更麻烦的是，不同厂商的空间架构差异很大，核心拓扑、网络结构、存储层次都不一样，针对一款硬件写的优化代码，完全无法移植到另一款上。每一代新硬件出来，都要重新编写一遍算子库，开发成本极高，生态建设速度非常缓慢。

**空间数据流架构通过分布式算力与片上直连网络有效缓解了传统架构的内存墙瓶颈，但硬件的透明化也将调度责任完全转移给了软件，导致可编程性成为落地的核心障碍** 。现有依赖手工优化库的模式既无法支撑新算子的快速落地，也难以适配不同代际的硬件演进，通用自动化的编译方案是行业的必然需求。

## 二、TileLoom 整体设计：把硬件调度权完全交给编译器

> 面对空间架构的可编程性难题，学术界和工业界已经探索了很多年，但大多是针对特定硬件的专用方案，缺乏通用且可移植的编译框架。 **TileLoom 的出现，正是为了填补这一空白， *它的核心思路是：既然空间架构的硬件行为是可预测的，那就可以把原本由 GPU 硬件完成的调度工作，全部交给编译器在编译期静态完成。***

### 2.1 核心洞察：空间架构的可预测性让静态编译成为可能

很多人会有疑问，为什么 GPU 不做静态调度，而空间架构可以？答案在于硬件的设计哲学不同。

GPU 为了通用性，把很多硬件细节都隐藏起来了，缓存是自动管理的，线程块是动态调度的，程序员和编译器都很难精准预测程序的实际执行时间。而且 GPU 需要支持复杂的分支、随机访存和上下文切换，硬件必须有足够的动态能力来应对各种场景。

**但空间数据流架构不一样，它的设计目标就是面向规整的张量计算，硬件结构完全暴露给软件** ：核心的数量和位置是固定的，片上网络的拓扑和带宽是固定的，本地存储的容量和带宽是固定的，计算单元的吞吐也是固定的。没有自动缓存，没有动态调度，所有行为都由软件指令决定。

这就意味着，只要有准确的硬件模型，编译器就可以在编译期精确估算不同映射方案的性能，从而搜索出最优的调度策略。 **硬件越透明、越规整，静态编译的效果就越好。 *TileLoom 正是基于这一洞察，构建了完整的自动化数据流规划体系。***

### 2.2 三层编译流水线：从 tile DSL 到硬件可执行文件

> TileLoom 的整体编译栈分为三个主要阶段，全程基于 MLIR 基础设施构建，通过多层中间表示实现不同层级的优化解耦。

![图 2：TileLoom 整体框架总览。该图完整展示三层端到端编译流水线，串联 tile 级 DSL 输入到硬件可执行程序全链路，分为前端、数据流规划、后端代码生成三大核心模块。](https://mmbiz.qpic.cn/sz_mmbiz_png/GxIgp4icchHibRiceiaC6FeicAiamjyt0BS1xw7TGhKaT1lGbeeMExiamg1vKCdOTLoF3lvLHNfwLM4FBrIEBc36LOnIgwUGXw3oVECeOBiaUNxC3rM/640?wx_fmt=png&from=appmsg&watermark=1#imgIndex=5)

图 2：TileLoom 整体框架总览。该图完整展示三层端到端编译流水线，串联 tile 级 DSL 输入到硬件可执行程序全链路，分为前端、数据流规划、后端代码生成三大核心模块。

**第一个阶段是前端处理** 。前端接收用户编写的 tile 级内核，目前支持 Triton 和 Helion 两种 DSL。前端首先会探索候选的 tile 形状，也就是分块的大小和布局，这一步和传统的自动调优过程类似。然后前端会把 tile 程序降低到统一的 MLIR 中间表示，经过归一化处理后，形成数据流无关的 IR。

所谓数据流无关，就是指这一层的 IR 只描述了计算的逻辑结构和 tile 网格的划分，还没有绑定任何具体的硬件映射，既可以降到 GPU 上运行，也可以交给后面的数据流规划阶段处理。

**第二个阶段是数据流规划，这也是 TileLoom 最核心的部分** 。这个阶段接收数据流无关的 IR 和目标硬件的描述，然后做两件事：

- 一是时空映射，决定逻辑 tile 网格怎么映射到物理核心阵列，哪些 tile 并行执行，哪些串行执行，顺序如何；
- 二是数据移动规划，分析数据的复用机会，决定哪些数据可以通过片上广播共享，哪些数据可以缓存在本地复用，以及数据在存储层次中的位置和传输时机。

所有可行的映射和数据移动方案共同构成了庞大的设计空间，TileLoom 会通过性能模型对每个候选方案进行评估，选出排名靠前的 top-k 个方案，交给后续阶段处理。

**第三个阶段是后端代码生成** 。选中的 top-k 方案会被转换成数据流感知的 IR，也就是所有的内存分配、数据拷贝、通信端点都已经确定。然后经过块级优化，最终降低到目标硬件的后端接口，生成每个核心的可执行程序。TileLoom 本身不做核内的指令级优化，这部分工作复用厂商已有的后端工具，它只负责核间的数据流规划。

### 2.3 两阶段选择策略：模型预筛与硬件实测的平衡

> 这里有一个很现实的问题：静态性能模型不可能百分之百准确，尤其是很多微架构细节厂商不会公开，模型总会有误差。如果只选模型预测最优的那一个方案，很可能因为模型偏差导致实际性能不是最好的。

为了解决这个问题，TileLoom 采用了可选的两阶段选择策略。

- 第一阶段用自动生成的性能模型对所有候选方案排序，选出排名靠前的 k 个方案；
- 第二阶段把这 k 个方案都编译成可执行文件，在真实硬件上实际运行测试，最终选出性能最好的那一个。

***这种策略的好处是兼顾了编译效率和最终性能。设计空间虽然大，但大部分劣质方案都会被性能模型过滤掉，只有少数高质量候选才需要实际跑测*** 。k 值可以根据需求调整，k 越大，找到最优解的概率越高，但编译和调优的时间也越长。

**论文中的实验显示，在 8×8 的 Mesh 拓扑上，top-2 就能收回大部分收益，top-3 之后性能增长就很平缓了，而编译时间几乎是线性增长的** 。对于大多数场景，选择 k 等于 2 或者 3，就能在编译时间和性能之间取得很好的平衡。

**TileLoom 的核心创新在于将 GPU 时代由硬件隐式完成的网格调度、数据复用工作，转化为编译期的静态优化问题** 。 ***基于 MLIR 的多层 IR 设计既保证了前端的通用性，也支撑了后端的多硬件适配*** ，两阶段选择策略则在编译效率与最终性能之间找到了合理的平衡。

## 三、时空映射：将逻辑 tile 网格铺展到物理核心阵列

> 时空映射是数据流规划的第一步，也是最基础的一步，它决定了计算任务在空间和时间上的基本分布。 简单来说，时空映射要回答的问题就是：这么多 tile ，哪些分给不同的核心同时算，哪些留给同一个核心依次算，按什么顺序来算。

### 3.1 统一中间表示：用仿射循环统一所有 tile 计算

在做映射之前，首先要有一个统一的程序表示。 **TileLoom 使用仿射循环来描述 tile 级的并行与串行结构** ，这也是整个数据流规划的基础。

我们可以用矩阵乘法来举例子。一个 m×k×n 的矩阵乘法，输出是 m 行 n 列的矩阵。如果用输出固定的分块方式，把输出分成 64×64 的 tile ，那么逻辑上就有两个并行维度：x 维度对应输出的行方向，y 维度对应输出的列方向，每个 tile 对应一个独立的输出块。

***在 TileLoom 的中间表示里，这两个并行维度用最外层的 affine.parallel 循环来表示，循环变量就是每个 tile 的编号*** 。循环内部是一个 scf.for 循环，沿着 k 维度迭代累加，代表单个 tile 内部的串行计算。所有的内存访问地址，都被表示成群变量和片内索引的仿射函数，也就是线性组合加偏移的形式。

![清单 1：数据流规划阶段前的 MLIR 中间表示代码
。该清单是前端输出的硬件无关标准化 MLIR，以矩阵乘法为示例展示统一抽象层，采用 affine.parallel 表示逻辑分块网格，scf.for 完成 K 维度规约计算，linalg 算子描述单块内部张量运算，所有内存访存地址统一转换为仿射表达式。此时代码仅定义逻辑分块与计算逻辑，未绑定任何物理核心、片上网络、存储资源，是 TileLoom 实现跨硬件通用编译的底层基础。](https://mmbiz.qpic.cn/sz_mmbiz_png/GxIgp4icchH9ONM7tP15U3sooAwJqaTohcweKPsZX13ZSpOGvmMtyRUzHqgic2cdQ3xECneD75AYTrCMuysaunsZIzf3nZsAK54AxMc7e2Bdw/640?wx_fmt=png&from=appmsg&watermark=1#imgIndex=6)

清单 1：数据流规划阶段前的 MLIR 中间表示代码 。该清单是前端输出的硬件无关标准化 MLIR，以矩阵乘法为示例展示统一抽象层，采用 affine.parallel 表示逻辑分块网格，scf.for 完成 K 维度规约计算，linalg 算子描述单块内部张量运算，所有内存访存地址统一转换为仿射表达式。此时代码仅定义逻辑分块与计算逻辑，未绑定任何物理核心、片上网络、存储资源，是 TileLoom 实现跨硬件通用编译的底层基础。

**这种表示方式的好处是结构非常规整，所有的并行和串行关系都清晰地体现在循环嵌套结构里** ，非常适合做后续的分析和变换。不管前端是 Triton 还是其他 tile 语言，最终都会被归一化成这种统一的结构。

### 3.2 映射设计空间：三个耦合决策定义全部数据流向

> 有了统一的循环表示之后， **时空映射的本质，就是把原来的并行循环，拆分成空间并行和时间串行两部分** 。具体来说，设计空间由三个相互耦合的选择决定。

- 第一个选择， **是把哪些原始并行维度映射到空间维度** 。比如 2D 的 Mesh 有 x 和 y 两个空间维度，我们可以把原始的 m 维度映射到空间 x，把 n 维度映射到空间 y，这样两个维度都并行执行；也可以只把其中一个维度映射到空间，另一个维度留作时间串行。每映射一个维度到空间，就相当于把这个维度的循环按空间大小分块，外层生成对应硬件索引的并行循环。
- 第二个选择， **是分块的顺序** 。当一个并行维度被多个空间维度分块时，分块的先后顺序会影响 tile 在核心阵列上的空间布局。不同的布局会带来不同的数据复用模式和通信代价，比如行优先和列优先的布局，对应的广播方向就完全不一样。
- 第三个选择， **是剩余并行维度的时间顺序** 。当所有空间维度都用完之后，剩下的并行维度就会变成时间维度，用串行循环来实现。这些时间循环的排列顺序，会决定 tile 在时间上的执行次序，进而影响时间复用的效果和片上网络的流量形态。

TileLoom 会枚举所有合法的组合，生成不同的循环嵌套结构，每一种结构对应一种时空映射方案，后续的复用分析和性能评估都会基于这些结构展开。

![清单 2：时空映射完成后的循环结构代码
。该清单展示逻辑分块网格到物理硬件核心的循环转换结果，最外层 affine.parallel 直接映射 8×8 物理核心阵列，新增 tx、ty 两层时间循环表示多波次分块迭代，内层 scf.for 仍保留核内串行规约逻辑。时空映射的核心转化逻辑是将原始逻辑并行循环拆分为空间并行硬件循环与时间波次循环，所有原始并行维度二选一分配至空间层或时间层。](https://mmbiz.qpic.cn/sz_mmbiz_png/GxIgp4icchH86Xxl9deibOHGxRFtFRoXr1yHEa18cW4TRutlD3jiaqaUbJDXW31t48DfIibn2NuTHNcWwdM4HiaTfXklOtz1hmOeLX1tCEDMkae8/640?wx_fmt=png&from=appmsg&watermark=1#imgIndex=7)

清单 2：时空映射完成后的循环结构代码 。该清单展示逻辑分块网格到物理硬件核心的循环转换结果，最外层 affine.parallel 直接映射 8×8 物理核心阵列，新增 tx、ty 两层时间循环表示多波次分块迭代，内层 scf.for 仍保留核内串行规约逻辑。时空映射的核心转化逻辑是将原始逻辑并行循环拆分为空间并行硬件循环与时间波次循环，所有原始并行维度二选一分配至空间层或时间层。

### 3.3 分块式映射原则：保留数据局部性是核心前提

> 值得注意的是，TileLoom 采用的是基于分块的映射策略，也就是连续的逻辑 tile 会被映射到连续的物理核心，或者连续的时间片上。 **为什么不做更灵活的映射？**

**核心原因是为了保留数据局部性** 。在大多数张量计算中，逻辑上相邻的 tile 往往会共享部分数据，比如矩阵乘法中同一行的输出 tile 会共享同一块 A 矩阵数据，同一列的输出 tile 会共享同一块 B 矩阵数据。如果把相邻的 tile 放在相邻的核心上，就可以很方便地利用片上网络做广播和传递，数据复用的成本很低。

如果采用随机映射或者打散的映射，虽然理论上可以做负载均衡，但相邻 tile 的局部性被破坏了，数据共享会变得非常困难，通信成本会急剧上升，最终整体性能反而更差。对于规整的张量计算来说，分块式映射是兼顾局部性和实现复杂度的合理选择。

时空映射的本质是对逻辑 tile 的迭代空间进行划分，将其拆解为空间并行与时间串行两部分。三个耦合的决策共同定义了 tile 在核心阵列上的空间布局与时间次序，而基于分块的映射策略，则是保证数据局部性、支撑后续数据复用优化的核心基础。

## 四、数据复用规划：榨干片上网络与分布式缓存的每一分价值

> 如果说时空映射决定了计算的分布，那么数据复用规划就决定了数据的流动方式，也是 TileLoom 带来性能提升的核心来源。 ***空间架构的最大优势就是片上带宽高、通信成本低，* 而能不能把这个优势发挥出来，关键就在于能不能最大化数据复用，减少对片外显存的访问。**

### 4.1 仿射访问分析：自动识别时空复用机会

要做数据复用，首先要知道哪些数据是可以复用的。TileLoom 基于仿射访问分析来自动识别复用机会，方法非常巧妙。

在时空映射之后的循环结构里，有三类循环变量：

- 空间循环变量，对应不同的核心；
- 时间循环变量，对应不同的 tile 波次；
- 还有串行循环变量，对应单个 tile 内部的计算迭代。

对于每一个内存加载操作，它的地址都是这些循环变量的仿射函数。如果一个加载操作的地址不依赖某个空间循环变量，那就说明在这个空间维度上，所有核心用到的这块数据是完全一样的，这块数据就具备空间复用的潜力。同理，如果地址不依赖某个时间循环变量，那就说明在这个时间维度的多次迭代中，这块数据都是一样的，具备时间复用的潜力。

我们还是用矩阵乘法来举例。A 矩阵的 tile 地址只和输出的行索引以及 k 索引有关，和列索引无关。如果我们把输出行映射到空间 x 维度，列映射到空间 y 维度，那么 A 矩阵的地址就不依赖 y 空间变量，也就是说同一行的所有核心用到的 A tile 是一样的，就可以在行方向上做空间广播。同理，B 矩阵的地址不依赖 x 空间变量，就可以在列方向上做空间广播。

整个分析过程完全是自动的，不需要人工标注，编译器通过检查地址表达式里包含哪些循环变量，就能自动识别出所有的空间和时间复用机会。

### 4.2 空间复用：用片上广播替代重复全局访存

> 识别出空间复用机会之后，接下来就是怎么实现复用。最朴素的方式是每个核心自己从全局显存加载数据，这样最简单，但完全没有利用空间复用，全局带宽压力最大。

如果有空间复用，就可以用广播的方式：只让一个或者少数几个核心从全局加载数据，然后通过片上网络把数据广播给其他需要的核心。这样可以大幅减少全局显存的访问次数，把片外带宽的压力转移到片上网络上，而片上网络的带宽本来就远高于片外显存，刚好可以发挥空间架构的优势。

具体的广播实现方式有很多种。

- 如果只是一维复用，比如同一行的核心共享数据，那就可以沿着行方向做流水式传递，每个核心收到数据后复制一份本地使用，再传给下一个核心。
- 如果是二维复用，就可以先在行方向广播，再在列方向广播，或者采用波前式的传播模式。不同的模式对网络资源的占用、延迟和本地缓冲区的要求都不一样。
![清单 3：空间复用标注示例代码。
该清单在张量加载指令上增加广播类型与片上网络资源注解，矩阵 A 沿水平 NoC 行广播、矩阵 B 沿垂直 NoC 列广播，实现二维网格全阵列空间复用。传统基准方案每个核心独立从 DRAM 加载完整张量，本方案仅一次全局加载后通过片上网络多播分发张量，大幅削减离片带宽消耗。](https://mmbiz.qpic.cn/mmbiz_png/GxIgp4icchHicW7PrGgiar2pibekhBCQhmwBJqvWQOvzQUmGSIFgPK8jKHyNlQC5nMvwMD1jSZ7SdlTwmzQQ4yWsUbdDUqEUfFDibt60GibsPLO4E/640?wx_fmt=png&from=appmsg&watermark=1#imgIndex=8)

清单 3：空间复用标注示例代码。 该清单在张量加载指令上增加广播类型与片上网络资源注解，矩阵 A 沿水平 NoC 行广播、矩阵 B 沿垂直 NoC 列广播，实现二维网格全阵列空间复用。传统基准方案每个核心独立从 DRAM 加载完整张量，本方案仅一次全局加载后通过片上网络多播分发张量，大幅削减离片带宽消耗。

**TileLoom 不固定使用某一种广播策略，而是会根据硬件的网络描述，枚举所有合法的广播模式，包括完全不广播的基线方案** 。所有内存操作的不同实现方式组合起来，就形成了数据移动的设计空间，最后由性能模型来评估选出最优的组合。

### 4.3 时间复用：循环提升与缓冲区容量的权衡

> 除了空间上的跨核心复用， **数据还可以在时间上复用** ，也就是同一个核心把数据加载到本地缓存之后，在后续的多次迭代中重复使用，不用每次都重新加载。

时间复用是通过循环提升来实现的。简单来说，就是把加载操作从内层循环移到外层循环，这样加载的次数就减少了，但数据需要在本地缓存里保留更长的时间，占用更多的缓冲区空间。

循环提升有两种情况。

- **一种是提升到不相关的循环外面** ，比如 A 矩阵的加载不依赖 tn 循环，那把它提到 tn 循环外面，加载次数会减少，但占用的缓冲区大小不变，因为每次加载的还是同样大小的数据，只是用的次数多了。 ***这种提升是无代价的，只要有足够的缓冲区就应该做。***
- **另一种是提升到相关的循环外面** ，比如把 A 矩阵的加载提到 k 循环外面，那原来每次迭代加载一块 k tile ，现在就要一次性加载所有 k 对应的 tile ，缓冲区占用会成倍增加，但加载的次数也会大幅减少。 ***这种提升就需要权衡，缓冲区占用不能超过硬件的本地存储容量。***

TileLoom 会枚举每个加载操作所有合法的提升层级，计算每个层级对应的缓冲区占用，剔除掉超过硬件容量的选项，剩下的都作为候选方案进入设计空间。

### 4.4 时空复用正交组合：最大化数据的复用次数

> 空间复用和时间复用不是互斥的，它们是正交的，可以组合使用。

- 一块数据可以只做空间复用，也就是广播一次就用完丢掉；
- 也可以只做时间复用，也就是每个核心自己加载然后多次使用；
- 还可以既做空间广播，又做时间缓存，广播一次之后在本地保留多个迭代，最大化每一次数据加载的价值。
![清单 5：二维网格架构下矩阵乘法内核、筛选前适配数据流的 MLIR 代码片段示例
。每个 alloc、load、store 操作绑定 df 方言定义的物理硬件资源，性能模型可直接提取资源占用信息，分别计算计算、网络、存储三类操作延迟。](https://mmbiz.qpic.cn/sz_mmbiz_png/GxIgp4icchHic8jj6LnlRvGvVbP6lN6icziblaaTNlFBcTeT4GORMwyto4gxyMkJVRKQ58ElC1K5V4ciahey6bZvFZyRrL24rZLXopIvduiaKV4yU/640?wx_fmt=png&from=appmsg&watermark=1#imgIndex=9)

清单 5：二维网格架构下矩阵乘法内核、筛选前适配数据流的 MLIR 代码片段示例 。每个 alloc、load、store 操作绑定 df 方言定义的物理硬件资源，性能模型可直接提取资源占用信息，分别计算计算、网络、存储三类操作延迟。

***把所有的空间复用模式和时间提升层级组合起来，再加上时空映射的不同方案，就构成了完整的数据流设计空间。* TileLoom 会自动剔除掉违反存储容量约束的方案，剩下的候选方案都交给性能模型进行量化评估。**

这也是 TileLoom 和传统手工优化的最大区别：手工优化通常只会选择一两种固定的数据流模板，比如 1D 广播或者 2D 广播，然后用启发式规则来选择。 **但 TileLoom 会搜索整个设计空间，找到针对具体算子形状和硬件参数的最优组合，尤其是在不规则形状的计算任务上，优势会更加明显。**

基于仿射访问的复用分析能够自动识别所有时空复用机会，通过空间广播将片外访存压力转移到高带宽的片上网络，通过时间循环提升最大化本地缓存的复用效率。两者的正交组合能够充分挖掘数据的复用潜力，是空间架构下提升性能的核心手段。

## 五、统一硬件抽象：用一套 IR 描述所有空间数据流架构

> **TileLoom 的目标** 不是只支持某一款硬件，而是 **要成为通用的空间数据流架构编译框架。** 要做到通用，首先就要有一套统一的方式来描述不同的硬件，不能把硬件参数硬编码到优化逻辑里。

### 5.1 为什么需要可复用的硬件表示

在现有的很多空间架构编译方案里，硬件的拓扑、存储、计算能力都是硬编码在编译器中的。换一款芯片，就要改很多优化逻辑，甚至要重写整个编译器。这不仅开发成本高，也很难做架构探索和跨代移植。

TileLoom 的做法是把所有硬件信息都抽离出来，用专门的硬件表示语言来描述，编译器的优化 pass 只读取这个表示，不依赖任何具体的硬件参数。这样一来，要支持新的硬件，只需要写一份新的硬件描述，不用改编译器本身的优化逻辑。

不仅如此，分层的硬件表示还可以让不同的编译阶段使用不同层级的信息。比如时空映射只需要知道核心阵列的拓扑和网络带宽，不需要知道核内的计算单元细节；数据移动规划需要知道存储层次和连接关系；性能模型才需要核内计算单元的具体吞吐。每个阶段只关心自己需要的信息，架构更清晰，复用性也更好。

### 5.2 df 方言的三层抽象栈：从拓扑到计算单元

> TileLoom 设计了一套自定义的 MLIR 方言，叫做 df 方言，用来描述硬件结构。整个表示分为三层抽象，从粗到细分别对应不同的编译阶段。

**最上层是扩展架构层，也就是核间的空间结构** 。这一层有三个核心的操作。

- df.spatial\_dim 用来声明一个抽象的空间维度，可以理解为一个索引轴，用来复制硬件组件；
- df.core 用来声明一组核心，通过 scaleout 参数绑定到空间维度上，形成核心阵列；
- df.interconnects 用来声明连接组件的网络，通过仿射映射描述网络的连接拓扑，同时指定每条链路的带宽。

比如描述一个 8×8 的 2D Mesh，只需要先声明 x 和 y 两个大小为 8 的空间维度，再声明核心阵列的 scaleout 是(x,y)，最后分别声明水平和垂直两个方向的互联网络，指定相邻核心的连接关系和带宽就可以了。

![清单 6：带抽象扩展层的二维网格硬件 df 方言描述
。通过 df.spatial_dim 定义 8×8 硬件网格，df.core 声明全局核心阵列，df.interconnects 描述水平、垂直环形互联的带宽与环绕拓扑映射规则。](https://mmbiz.qpic.cn/sz_mmbiz_png/GxIgp4icchHibexfW4s0fEOcQgic0FhVEJ34uenz1iaJ4ugia9h3Vf5CQCS8Xb6vOdWyMaHmuszvfqmCC1eAUJFUBhqkoqrfqgQ7kBVdo9BpSY8M/640?wx_fmt=png&from=appmsg&watermark=1#imgIndex=10)

清单 6：带抽象扩展层的二维网格硬件 df 方言描述 。通过 df.spatial\_dim 定义 8×8 硬件网格，df.core 声明全局核心阵列，df.interconnects 描述水平、垂直环形互联的带宽与环绕拓扑映射规则。

**中间层是存储与数据移动层** 。这一层增加了 df.memory 操作，用来声明一组存储器，同样通过空间维度来索引，指定每个存储实例的容量和带宽。还有 df.mux 操作，用来描述组件之间的连接关系，比如每个核心连接到自己对应的本地 L1 缓存，或者几组核心共享一个 DRAM 通道。

![清单 7：包含扩展核心、本地暂存器与 DRAM 的完整二维网格 df 方言描述。该完整硬件描述供给数据复用规划、内存分配模块使用，可精准计算张量缓存占用大小、DRAM 访问带宽、跨核传输链路竞争情况。](https://mmbiz.qpic.cn/mmbiz_png/GxIgp4icchHicHFawtUgm8d1MiaWhhuLrvulJJUy8bbEdbxia2Hjmkfp9glbT3nAmKpwYg1HtIsAic2EsMRyxFJUciaueqgbdY7v1qRgEftyZNcTQ/640?wx_fmt=png&from=appmsg&watermark=1#imgIndex=11)

清单 7：包含扩展核心、本地暂存器与 DRAM 的完整二维网格 df 方言描述。该完整硬件描述供给数据复用规划、内存分配模块使用，可精准计算张量缓存占用大小、DRAM 访问带宽、跨核传输链路竞争情况。

有了存储描述之后，编译器就可以知道数据可以放在哪里，不同存储之间的传输带宽是多少，从而规划数据的存放位置和移动路径。

**最下层是核内计算模型层** 。这一层描述每个核心内部的计算资源，包括 df.mat 矩阵单元、df.vec 向量单元、df.scalar 标量单元，分别指定它们的运算形状和吞吐率。

![清单 8：核内架构额外规格 df 方言描述
。该清单定义单核心内部计算单元硬件参数，df.mat 矩阵单元、df.vec 向量单元分别标注运算硬件尺寸与持续吞吐，绑定至 df.core 的 scalein 参数，专供性能模型计算单块 tile 串行计算延迟。](https://mmbiz.qpic.cn/sz_mmbiz_png/GxIgp4icchH8bGgbyDvey5pmEBZAR6kKBBz2ibCOc3JkbhBc7RU35FDONFpCWib9CTacGUe3lftEVJvqaJ8Dt2xYWjve0ia802jvia1DBadxT5iaU/640?wx_fmt=png&from=appmsg&watermark=1#imgIndex=12)

清单 8：核内架构额外规格 df 方言描述 。该清单定义单核心内部计算单元硬件参数，df.mat 矩阵单元、df.vec 向量单元分别标注运算硬件尺寸与持续吞吐，绑定至 df.core 的 scalein 参数，专供性能模型计算单块 tile 串行计算延迟。

这些信息会通过 df.core 的 scalein 参数绑定到核心上，供性能模型估算计算耗时。

### 5.3 拓扑无关性：从 2D Mesh 到 1D 环形的平滑适配

> 这套表示的表达能力很强，不止能描述 2D Mesh 结构，其他拓扑的空间架构也都可以描述。论文里举了一个 1D 三环架构的例子，类似 IBM 的 Spyre 加速器，用 df 方言可以很方便地建模。

![图 3：使用 df 方言建模的一维三环架构示例
。该图用于验证论文提出的 df 硬件描述方言具备跨拓扑通用建模能力，打破论文主力实验平台二维网格的局限，证明编译器不局限于 Tenstorrent 架构。](https://mmbiz.qpic.cn/sz_mmbiz_png/GxIgp4icchHicfhK8Olw7cbfBfVwVqeT7QfUunPaabkMSF9DYUqbzIHPXqyr83ndsNt7DhTdHCJzUwjibPhQPibpNG2PasX03WrhrAnaemiaQTRU/640?wx_fmt=png&from=appmsg&watermark=1#imgIndex=13)

图 3：使用 df 方言建模的一维三环架构示例 。该图用于验证论文提出的 df 硬件描述方言具备跨拓扑通用建模能力，打破论文主力实验平台二维网格的局限，证明编译器不局限于 Tenstorrent 架构。

![清单 9：描述一维三环架构的 df 方言代码示例
。该清单完整使用 df 分层语法建模非二维网格拓扑，包含一维 32 核心阵列、双层环形互联、独立 L1 存储与 DRAM 通道，核心作用是验证 df 方言的通用建模能力，证明其不局限论文主力实验的二维 Mesh 架构。](https://mmbiz.qpic.cn/mmbiz_png/GxIgp4icchH8zELNOe3OugmXT3Ejxdr7VDn6D59JcwasJiabA0ymkHxaicovdAtTXaqBAunMFQLHoiaNLQdQwT1IABowbsSiasrVOqKcGibD8qJZ8/640?wx_fmt=png&from=appmsg&watermark=1#imgIndex=14)

清单 9：描述一维三环架构的 df 方言代码示例 。该清单完整使用 df 分层语法建模非二维网格拓扑，包含一维 32 核心阵列、双层环形互联、独立 L1 存储与 DRAM 通道，核心作用是验证 df 方言的通用建模能力，证明其不局限论文主力实验的二维 Mesh 架构。

这种拓扑无关性带来了两个非常重要的价值。

- **第一个价值是硬件移植成本低** ，不管是新的芯片代际，还是完全不同厂商的架构，只要能用 df 方言描述出来，TileLoom 的编译流水线就可以直接工作，不需要重写优化逻辑。
- **第二个价值是支持软硬件协同设计** 。架构师可以快速修改硬件描述，比如调整核心数量、网络带宽、缓存大小，然后用 TileLoom 编译运行典型算子，评估性能变化，从而在芯片设计阶段就找到最优的架构参数，不用等到流片之后再验证。

基于 MLIR 的 df 方言通过三层抽象栈统一了空间数据流架构的硬件描述，将编译器优化逻辑与具体硬件参数解耦，既支撑了多架构的可移植性，也为软硬件协同设计提供了统一的评估框架，是 TileLoom 通用性的核心基础。

## 六、性能建模：编译期精准预测映射的实际执行耗时

> 设计空间里有这么多候选方案，怎么判断哪个好哪个坏？这就需要性能模型来给每个方案打分。 性能模型的准确性，直接决定了编译器能不能选出真正优秀的映射方案。

### 6.1 分层成本聚合：从内层循环向外逐层估算

TileLoom 的性能模型采用分层估算的方式，从最内层的循环体开始，一层一层向外聚合，最终得到整个程序的总执行时间。

最内层是单个循环体的计算耗时。模型首先把每个高级算子拆解成核心的计算指令，然后根据核内的计算单元参数，估算每个算子的执行时间。对于可以并行执行的独立算子，取耗时最长的那个作为段耗时；对于有依赖的算子，时间相加。

**这个计算模型不是周期精确的，它不会模拟具体的指令调度和流水线细节，只是从宏观上估算计算的吞吐** 。论文里也提到， ***这个粒度的模型足够区分计算密集和访存密集的映射，也足够用来比较不同数据流方案的优劣，完全可以满足排序的需求。***

### 6.2 计算访存重叠：双缓冲流水线的时序模型

> 光有计算和访存的单独耗时还不够，现代处理器都会用双缓冲甚至多缓冲技术，让计算和数据传输并行执行。TileLoom 的性能模型也考虑了这一点，采用流水线模型来估算重叠后的总耗时。

对于一个有 I 次迭代的内层循环，总执行时间的估算公式为：

其中， 是循环的迭代次数， 是单次迭代中所有加载操作的总耗时， 是单次迭代中计算部分的耗时， 是单次迭代中所有存储操作的总耗时。

这个公式可以分成两部分理解。

- 第一部分是中间的 次稳态迭代，这时候流水线已经填满，加载下一批数据、计算当前批次、存储上一批结果可以同时进行，所以每轮的耗时由最慢的环节决定，也就是访存总耗时和计算耗时的最大值。
- 第二部分是流水线的填充和排空开销，也就是开头两次和结尾两次迭代的额外时间。当迭代次数很多的时候，这部分开销占比很小，整体性能就由稳态的瓶颈决定。

这个模型的本质和工厂流水线是一样的：正常运转的时候产能由最慢的工序决定，只有开机和停机的时候会有额外的损耗。

![图 4：矩阵乘法流水线执行示意图。该图展示 TileLoom 性能模型的核心流水线重叠假设，建模单循环迭代内加载、计算、存储三类操作并行执行行为，采用双缓冲机制掩盖片上网络与 DRAM 传输延迟。模型区分流水线填充、稳态迭代、流水线排空三个阶段，稳态场景下加载存储总耗时与计算耗时取最大值作为性能瓶颈，精准区分带宽受限、算力受限两类算子场景。](https://mmbiz.qpic.cn/mmbiz_png/GxIgp4icchH8H780e0lRQNjxElKlW1c1z0DEGzeYPELLaTYbeVdueDwrDbcrhwmCicicD4icO41OaS7oq1QYSaek22R1IDwytVcMcib8V6CYQNZ0/640?wx_fmt=png&from=appmsg&watermark=1#imgIndex=15)

图 4：矩阵乘法流水线执行示意图。该图展示 TileLoom 性能模型的核心流水线重叠假设，建模单循环迭代内加载、计算、存储三类操作并行执行行为，采用双缓冲机制掩盖片上网络与 DRAM 传输延迟。模型区分流水线填充、稳态迭代、流水线排空三个阶段，稳态场景下加载存储总耗时与计算耗时取最大值作为性能瓶颈，精准区分带宽受限、算力受限两类算子场景。

### 6.3 网络拥塞建模：并发传输下的有效带宽折算

> 另一个不能忽略的因素是网络拥塞。当多个数据传输同时使用同一段网络链路的时候，实际可用带宽会下降，传输时间会变长。如果不考虑拥塞，模型的估算会过于乐观。

**TileLoom 的处理方式是，根据每个内存操作绑定的网络资源，把占用相同链路的操作分到一组，然后把链路的总带宽按比例分配给组内的所有操作** 。每个操作的有效带宽等于链路带宽除以同时使用这条链路的操作数量，传输时间就等于数据量除以有效带宽。

对于全局访存操作，模型假设访问是比较随机的，流量会均匀分布在整个网络上；对于广播操作，根据广播模式的不同，占用的链路也不同，比如行广播只占用水平链路，列广播只占用垂直链路。这些信息都会在数据移动规划阶段标注在内存操作上。

最后，使用不同链路的传输可以并行执行，使用相同链路的传输分时执行，把所有传输时间组合起来，就得到了单次迭代的总加载和总存储耗时。

### 6.4 定位与精度：排序正确比周期精准更重要

论文中的验证显示，TileLoom 的性能模型预测值和实际测量值的几何平均误差在 17%左右。这个数字看起来不小，但对于编译器的场景来说已经足够了。

因为性能模型的核心目标不是精确预测运行时间，而是给候选方案正确排序，找出性能最好的那一批。只要排序是对的，哪怕绝对值有偏差，也能选出好的映射方案。尤其是对于计算密集和访存密集的分界，模型捕捉得非常准确，这就足够支撑顶层的设计空间搜索了。

再加上后面还有可选的硬件实测调优阶段，模型的小误差完全可以通过少量的实测来修正。这种设计思路非常务实：不追求极致的模型精度，而是用模型做粗筛，用实测定终局，在工程上是非常合理的权衡。

TileLoom 的性能模型采用分层聚合的估算方式，结合双缓冲流水线模型与网络拥塞建模，能够在编译期对不同数据流方案的性能做出有效评估。它的定位不是周期精确的仿真器，而是可靠的方案排序器，配合 top-k 实测调优，在编译效率与优化效果之间取得了极佳的平衡。

## 七、实验验证：两代 Tenstorrent 硬件上的真实表现

> 所有的设计和优化，最终都要落到实际性能上才有意义。 研究团队在 Tenstorrent 的两代硬件上做了全面的实验验证，覆盖了多个典型的 AI 算子，也做了细致的消融研究。

### 7.1 测试平台：Wormhole 与 Blackhole 的硬件规格

实验使用了两款 Tenstorrent 的加速卡，分别是 Wormhole 和 Blackhole，代表两代不同的工艺和架构规模。

![表 1：实验所用 Tenstorrent Wormhole 与 Blackhole 硬件规格参数。](https://mmbiz.qpic.cn/mmbiz_png/GxIgp4icchHicYQjhDXlf9Lh8laEbzCcgVw7m2hb2YrwbuIicK2U5EXUe5BRV3Viaaaic7L8wFDHxnzYTsL0nOTNvfGzyQkibyUo6RDdsEAtyTrMM/640?wx_fmt=png&from=appmsg&watermark=1#imgIndex=16)

表 1：实验所用 Tenstorrent Wormhole 与 Blackhole 硬件规格参数。

因为厂商没有公开完整的硬件规格，研究团队通过微基准测试的方式，测得了矩阵单元吞吐、有效片上网络带宽、DRAM 带宽等关键参数，然后把这些实测参数写入 df 硬件描述，供性能模型使用。这种做法也非常符合工程实际：不需要完整的芯片文档，通过实测就能得到足够的建模参数。

编译工作在配备双路 16 核 Xeon Gold 6326 CPU 和 512GB 内存的主机上完成，实验中的所有性能结果，都是仅用性能模型选出的最优方案，没有经过最后的硬件调优阶段，也就是说这些收益完全来自 TileLoom 的静态规划能力。

### 7.2 核心算子性能：四大典型算子的横向对比

> 实验选择了四个非常有代表性的算子：GEMM、FlashAttention、Flash Decode 和 Mamba Chunk Scan，覆盖了从计算密集到访存密集的不同类型，也包含了厂商有优化和没优化的不同场景。

#### 7.2.1 FlashAttention，两代硬件上均接近2倍性能提升

![图 5：TileLoom 与 TTNN 在 FlashAttention 算子上的相对性能对比，参数为（批次 B、序列长度 L、头数 N）
。在 Wormhole 与 Blackhole 两款硬件上，TileLoom 稳定实现 1.88~2.06 倍加速。性能增益核心来源是自动时空映射挖掘 Key、Value 张量跨核空间复用，通过片上网络广播替代重复 DRAM 加载，大幅削减离片内存流量。原厂 TTNN 库采用固定分块启发式策略，无法动态适配不同序列长度下的复用机会；而 TileLoom 依托硬件模型全局搜索最优广播方案，自动平衡片上缓存容量与网络传输流量。Blackhole 算力带宽比更高，内存瓶颈更突出，加速收益相比 Wormhole 更显著，直接验证数据流复用优化对高算力密度新一代 AI 加速器的核心价值。](https://mmbiz.qpic.cn/mmbiz_png/GxIgp4icchH97iawG5stNrvSlL6fyLSvMxlr5VqODmw9p991uhGPYEiaM5lVc1Hefw1tW3lkr8g2iaoqKI0FYYF3eYu1OrSFU2oAZUcia2X3f3jk/640?wx_fmt=png&from=appmsg&watermark=1#imgIndex=17)

图 5：TileLoom 与 TTNN 在 FlashAttention 算子上的相对性能对比，参数为（批次 B、序列长度 L、头数 N） 。在 Wormhole 与 Blackhole 两款硬件上，TileLoom 稳定实现 1.88~2.06 倍加速。性能增益核心来源是自动时空映射挖掘 Key、Value 张量跨核空间复用，通过片上网络广播替代重复 DRAM 加载，大幅削减离片内存流量。原厂 TTNN 库采用固定分块启发式策略，无法动态适配不同序列长度下的复用机会；而 TileLoom 依托硬件模型全局搜索最优广播方案，自动平衡片上缓存容量与网络传输流量。Blackhole 算力带宽比更高，内存瓶颈更突出，加速收益相比 Wormhole 更显著，直接验证数据流复用优化对高算力密度新一代 AI 加速器的核心价值。

#### 7.2.2 Flash Decode，约为厂商性能的85%

![图 6：TileLoom 与 TTNN 在 Flash Decode 算子上的相对性能对比，参数为（批次 B、头维度 H、序列长度 L、隐藏维度 D）
。Flash Decode 仅存在单查询块，并行维度仅来源于批次维度，数据映射搜索空间大幅收缩，该图直观体现通用编译框架与厂商专用手工优化库的性能差距。TTNN 针对解码场景手工定制跨核归约通信调度逻辑，而 TileLoom 无算子专属定制代码，仅依靠通用数据流规划模块生成实现，平均仅达到 TTNN 84%-87%性能。但该结果仍证明通用编译框架具备接近专用库的性能下限，无需为特殊算子单独开发底层硬件库。从消融实验视角，该算子成为验证 TileLoom 跨核归约通信调度模块有效性的基准，说明当空间复用机会稀缺时，框架仍能输出鲁棒、具备工程可用性的代码。](https://mmbiz.qpic.cn/sz_mmbiz_png/GxIgp4icchH9uz9uNxrTB8QNudrl0mpRJwYiajT4Ky0qzQUdvM5E12UdcMicsjdw3icaChUjykxgJwhhAULAjWicpayqP7nviboFQcvJibbv5HSpD4/640?wx_fmt=png&from=appmsg&watermark=1#imgIndex=18)

图 6：TileLoom 与 TTNN 在 Flash Decode 算子上的相对性能对比，参数为（批次 B、头维度 H、序列长度 L、隐藏维度 D） 。Flash Decode 仅存在单查询块，并行维度仅来源于批次维度，数据映射搜索空间大幅收缩，该图直观体现通用编译框架与厂商专用手工优化库的性能差距。TTNN 针对解码场景手工定制跨核归约通信调度逻辑，而 TileLoom 无算子专属定制代码，仅依靠通用数据流规划模块生成实现，平均仅达到 TTNN 84%-87%性能。但该结果仍证明通用编译框架具备接近专用库的性能下限，无需为特殊算子单独开发底层硬件库。从消融实验视角，该算子成为验证 TileLoom 跨核归约通信调度模块有效性的基准，说明当空间复用机会稀缺时，框架仍能输出鲁棒、具备工程可用性的代码。

#### 7.2.3 Mamba Chunk Scan，性能表现是几何平均 10–20+倍，部分配置达50+倍

![图 7：TileLoom 与未融合 TTNN 实现的 Mamba 分块扫描算子性能对比，参数包含序列长度 L、头数 N、头维度 H、分组数 G、隐藏维度 D、分块大小 C
。该图凸显 TileLoom 内核融合+自动数据流规划的双重性能增益，原厂 TTNN 未提供原生融合 Mamba 算子，仅能串联离散基础算子执行，中间张量反复读写 DRAM 造成巨大带宽开销；TileLoom 直接从 Triton/Helion 融合 tile 内核出发，全程在片上本地存储完成中间数据复用，实现 10~55 倍大幅加速。该实验证明面向 tile 级 DSL 的编译框架独特优势：厂商硬件库仅提供离散基础算子，Mamba、线性注意力等新兴深度学习算子难以快速高性能部署，而 TileLoom 支持用户自定义融合内核，自动完成跨核分发与通信优化，大幅降低新型 AI 算子在空间数据流加速器上的部署门槛。](https://mmbiz.qpic.cn/sz_mmbiz_png/GxIgp4icchHib6CiajiaMRNlAxWFgs3FzO6zGgo72Tf6ULicD6mNHelOgKK4KBhaEZR3zTf1hr9jVJl4giczWvRolLcRV1JTyvyShmEbWwj5T7aq4/640?wx_fmt=png&from=appmsg&watermark=1#imgIndex=19)

图 7：TileLoom 与未融合 TTNN 实现的 Mamba 分块扫描算子性能对比，参数包含序列长度 L、头数 N、头维度 H、分组数 G、隐藏维度 D、分块大小 C 。该图凸显 TileLoom 内核融合+自动数据流规划的双重性能增益，原厂 TTNN 未提供原生融合 Mamba 算子，仅能串联离散基础算子执行，中间张量反复读写 DRAM 造成巨大带宽开销；TileLoom 直接从 Triton/Helion 融合 tile 内核出发，全程在片上本地存储完成中间数据复用，实现 10~55 倍大幅加速。该实验证明面向 tile 级 DSL 的编译框架独特优势：厂商硬件库仅提供离散基础算子，Mamba、线性注意力等新兴深度学习算子难以快速高性能部署，而 TileLoom 支持用户自定义融合内核，自动完成跨核分发与通信优化，大幅降低新型 AI 算子在空间数据流加速器上的部署门槛。

#### 7.2.4 GEMM，Wormhole 上达厂商性能的95%；Blackhole 上反超 10%

![图 8：TileLoom 与 TTNN 的矩阵乘法性能对比，TT-1D、TT-2D 作为参考数据流模板
。该图对比三类矩阵乘法实现方案：厂商启发式 TTNN、固定一维/二维广播模板 TT-1D/TT-2D、TileLoom 自动搜索映射方案。标准规整矩阵尺寸下 TileLoom 性能持平或超越手工固定模板，不规则矩阵维度场景优势进一步放大。TTNN 依靠固定 M/N 维度比值阈值切换 1D/2D 广播，阈值临界点易做出次优选择；TileLoom 遍历分块尺寸、时空映射、循环提升多重组合，不受固定模板限制。Wormhole 算力受限场景下 TileLoom 略低于 TTNN（0.95 倍），Blackhole 带宽瓶颈场景下反超至 1.10 倍，充分说明硬件算力带宽比决定数据流优化收益上限，TileLoom 建模搜索机制可自适应不同硬件架构特性。](https://mmbiz.qpic.cn/mmbiz_png/GxIgp4icchH8DneN58cIJtWkUqWaCpUuwTMvW7uuRApxicWFwAEkCmU3ETlicAt5Ciap9nuGKrFDnvsiaBWWpIVA8yjD8kuNjr621lGGmoDk69hU/640?wx_fmt=png&from=appmsg&watermark=1#imgIndex=20)

图 8：TileLoom 与 TTNN 的矩阵乘法性能对比，TT-1D、TT-2D 作为参考数据流模板 。该图对比三类矩阵乘法实现方案：厂商启发式 TTNN、固定一维/二维广播模板 TT-1D/TT-2D、TileLoom 自动搜索映射方案。标准规整矩阵尺寸下 TileLoom 性能持平或超越手工固定模板，不规则矩阵维度场景优势进一步放大。TTNN 依靠固定 M/N 维度比值阈值切换 1D/2D 广播，阈值临界点易做出次优选择；TileLoom 遍历分块尺寸、时空映射、循环提升多重组合，不受固定模板限制。Wormhole 算力受限场景下 TileLoom 略低于 TTNN（0.95 倍），Blackhole 带宽瓶颈场景下反超至 1.10 倍，充分说明硬件算力带宽比决定数据流优化收益上限，TileLoom 建模搜索机制可自适应不同硬件架构特性。

![图 9：不规则尺寸矩阵乘法的性能对比，(a) 固定 M=N=32768、遍历 K 维度；(b) 固定 M=K=32768、遍历 N 维度。两张子图形成对照实验，区分规约维度 K 与输出并行维度 N 对最优数据流的不同影响。K 仅参与单核心串行计算，增减只会改变计算强度，TTNN 与 TileLoom 性能走势趋同，随矩阵增大逐步进入算力饱和区间。而 N 直接决定二维网格行列并行负载，N 较小时一维广播更高效，N 接近 M 时二维双向广播收益更高。TTNN 依靠固定 M/N 比值切换模板，阈值附近易选错方案；TileLoom 依托硬件性能模型全局遍历时空映射、广播策略，不受静态阈值约束，在不规则长宽矩阵下稳定优于 TT-1D、TT-2D 两套固定模板，证明建模搜索比人工启发式更适配多变张量尺寸。](https://mmbiz.qpic.cn/mmbiz_png/GxIgp4icchH9VjUxcUeErVzyS7R35ktafpf8GgW5qMeqTynOzfgFhfJjrolNRSNnuraDtoER6pUNe1dexh005eZORpCff6LMibrzylwibiauTqQ/640?wx_fmt=png&from=appmsg&watermark=1#imgIndex=21)

图 9：不规则尺寸矩阵乘法的性能对比，(a) 固定 M=N=32768、遍历 K 维度；(b) 固定 M=K=32768、遍历 N 维度。两张子图形成对照实验，区分规约维度 K 与输出并行维度 N 对最优数据流的不同影响。K 仅参与单核心串行计算，增减只会改变计算强度，TTNN 与 TileLoom 性能走势趋同，随矩阵增大逐步进入算力饱和区间。而 N 直接决定二维网格行列并行负载，N 较小时一维广播更高效，N 接近 M 时二维双向广播收益更高。TTNN 依靠固定 M/N 比值切换模板，阈值附近易选错方案；TileLoom 依托硬件性能模型全局遍历时空映射、广播策略，不受静态阈值约束，在不规则长宽矩阵下稳定优于 TT-1D、TT-2D 两套固定模板，证明建模搜索比人工启发式更适配多变张量尺寸。

### 7.3 消融实验：空间与时间复用的真实收益

> 为了量化各个优化的贡献，研究团队做了两组消融实验，分别关闭空间复用和时间复用，观察性能的变化。

![表 3：Wormhole 硬件下启用（TileLoom）/禁用（仅 DRAM 加载）空间复用的 GEMM 算力（TFLOPS）。](https://mmbiz.qpic.cn/mmbiz_png/GxIgp4icchH8yr5hmL6ClOCTdZE4QQalfh3cfvsM7OAbiaNic8pd03DDNCO7Xu1fQ5RFTQONwBiaxu4dTQCy6YGYxBBoaUfyvx1KH4KLiaj7jn0o/640?wx_fmt=png&from=appmsg&watermark=1#imgIndex=22)

表 3：Wormhole 硬件下启用（TileLoom）/禁用（仅 DRAM 加载）空间复用的 GEMM 算力（TFLOPS）。

**空间复用的消融实验显示，对于小尺寸的 GEMM，关闭空间复用后性能下降一半以上，速度只有原来的二分之一左右。随着矩阵尺寸变大，算术强度升高，计算逐渐成为瓶颈，空间复用的收益会逐渐下降，但平均仍然能减少 70%的 DRAM 访问** 。也就是说，哪怕在计算密集的场景下，空间复用不能直接提升太多速度，但能大幅降低内存压力，给并发的其他任务留出更多带宽，也能降低功耗。

![表 4：不同 top-k 调参设置下 TileLoom 归一化几何平均性能（相对 TTNN）与编译耗时（秒），Top-1 代表无额外硬件 profiling 的纯静态编译。](https://mmbiz.qpic.cn/mmbiz_png/GxIgp4icchHicJLfXBHQkK5CYAmxQ4Hv9yBEEzemnBLCHMptnlkqYxQwfSkcJ2lOcchibegGUkxord2WhFQ4j7lAFOKjfgoAYjYV0ESpiccv7nY/640?wx_fmt=png&from=appmsg&watermark=1#imgIndex=23)

表 4：不同 top-k 调参设置下 TileLoom 归一化几何平均性能（相对 TTNN）与编译耗时（秒），Top-1 代表无额外硬件 profiling 的纯静态编译。

**时间复用的消融实验显示，时间复用最多能带来 12%左右的性能提升，在 M 和 N 比较大、K 比较小的场景下收益更明显** 。因为这种场景下数据有更多的时间复用机会。当 K 很大的时候，时间复用的缓冲区占用太高，收益就不明显了。

![图 10：Wormhole 硬件上启用/禁用时间复用的归一化 GEMM 性能对比
。该消融图单独量化时间复用优化的独立性能收益，时间复用通过循环外提将张量块长期缓存于本地 L1 存储，减少重复全局 DRAM 加载。实验控制 K 维持小数值保证算子处于带宽受限区间，M、N 越大，可复用的张量块数量越多，时间复用最高带来 1.12 倍加速。当矩阵尺寸偏小、本地缓存可一次性容纳全部张量时，复用优化无性能增益，性能模型会自动舍弃该类低效映射方案。时间复用与空间复用属于正交互补优化：空间复用解决跨核心重复加载问题，时间复用解决单核心迭代内重复加载问题，二者组合可平均削减 70%以上 DRAM 访问量，是 TileLoom 缓解内存墙的两大核心优化手段。](https://mmbiz.qpic.cn/sz_mmbiz_png/GxIgp4icchH8vzwMvGs1l4SNW5IQ3MyTU5T2ZWbbSlic5BccP4qSUgYngpwyibMBdDNJstaaNjnE1FxFbOBic6GqTxE83FlOgcsqUia8nnVBwPpQ/640?wx_fmt=png&from=appmsg&watermark=1#imgIndex=24)

图 10：Wormhole 硬件上启用/禁用时间复用的归一化 GEMM 性能对比 。该消融图单独量化时间复用优化的独立性能收益，时间复用通过循环外提将张量块长期缓存于本地 L1 存储，减少重复全局 DRAM 加载。实验控制 K 维持小数值保证算子处于带宽受限区间，M、N 越大，可复用的张量块数量越多，时间复用最高带来 1.12 倍加速。当矩阵尺寸偏小、本地缓存可一次性容纳全部张量时，复用优化无性能增益，性能模型会自动舍弃该类低效映射方案。时间复用与空间复用属于正交互补优化：空间复用解决跨核心重复加载问题，时间复用解决单核心迭代内重复加载问题，二者组合可平均削减 70%以上 DRAM 访问量，是 TileLoom 缓解内存墙的两大核心优化手段。

![图 11：TileLoom 性能模型预测 GEMM 吞吐量与实测性能的验证对比
。该图验证论文自研分层性能模型的有效性，模型预测吞吐量与硬件实测值几何平均误差为 17%，但模型核心目标并非精准周期级仿真，而是正确排序不同映射方案、精准识别算力/带宽性能切换临界点。模型精准捕捉屋顶线模型趋势：小矩阵带宽受限、大矩阵算力饱和，预测曲线与实测曲线变化趋势完全对齐。少量预测偏差来源于未建模的底层微架构细节，论文通过 top-k 硬件二次调优机制弥补该缺陷，仅将模型筛选的最优若干候选上板实测，兼顾编译搜索速度与最终运行性能。粗粒度分层建模大幅降低搜索开销，使大规模时空映射空间遍历具备工程落地可行性。](https://mmbiz.qpic.cn/mmbiz_png/GxIgp4icchHibc2QibzkZw58jDB2uAKKttiagIibibp2Qoax0cAgqot1mEZZGLVqBV9k1qJsTCsImPfnwNBMluoe467USOkTGdNUMwGj32qQ2gxQ8/640?wx_fmt=png&from=appmsg&watermark=1#imgIndex=25)

图 11：TileLoom 性能模型预测 GEMM 吞吐量与实测性能的验证对比 。该图验证论文自研分层性能模型的有效性，模型预测吞吐量与硬件实测值几何平均误差为 17%，但模型核心目标并非精准周期级仿真，而是正确排序不同映射方案、精准识别算力/带宽性能切换临界点。模型精准捕捉屋顶线模型趋势：小矩阵带宽受限、大矩阵算力饱和，预测曲线与实测曲线变化趋势完全对齐。少量预测偏差来源于未建模的底层微架构细节，论文通过 top-k 硬件二次调优机制弥补该缺陷，仅将模型筛选的最优若干候选上板实测，兼顾编译搜索速度与最终运行性能。粗粒度分层建模大幅降低搜索开销，使大规模时空映射空间遍历具备工程落地可行性。

这两组实验也验证了我们之前的分析： **空间复用是性能提升的主要来源，时间复用是锦上添花的补充** 。两者结合起来，共同构成了 TileLoom 的数据复用优化体系。

### 7.4 模型精度与 top-k 调优的投入产出比

> 最后一组实验验证了性能模型的精度，以及 top-k 调优的收益。实验在三种不同的拓扑上测试，分别是完整的 8×8 Mesh、4×8 Mesh 和 1×8 环形，测试不同 k 值下的性能和编译时间。

结果显示，top-1 也就是纯静态编译的方案，在 8×8 Mesh 上大概比 TTNN 差 6%左右。增加到 top-2，差距就缩小到 1.4%，top-3 的时候基本追平，top-5 的时候能反超 0.9%。也就是说，大部分收益在 k 等于 2 的时候就已经拿到了，再增加 k 收益很小，但编译时间几乎是线性增长的。

这个结果说明 TileLoom 的性能模型排序质量非常高，第一名可能不是最优的，但前三名里基本一定包含最优解。实际使用的时候，选 k 等于 2 或者 3，只需要多花一倍多的编译时间，就能收回几乎所有的性能潜力，投入产出比很高。

不同拓扑的对比也很有意思，拓扑越大，核心越多，设计空间就越大，top-k 调优的收益也就越明显。反过来小拓扑的优化空间小，模型选出来的第一名就很接近最优了。

**在两代 Tenstorrent 硬件上的实验充分验证了 TileLoom 的有效性：访存密集型算子收益显著，成熟算子性能持平甚至反超，新算子能够通过融合获得数量级提升** 。消融实验量化了各项优化的实际贡献，top-k 调优则以很低的额外成本弥补了模型的少量误差，整体表现达到了实用级别的水平。

## 八、定位与对比：TileLoom 在编译技术版图中的位置

> TileLoom 不是第一个做空间架构编译的工作，但它的定位和之前的工作有很明显的区别。 把它放在相关研究的版图里对比，能更清楚地看到它的创新和价值。

### 8.1 与硬件协同设计工具的差异

**第一类相关工作是硬件建模与协同设计工具** ，比如 Timeloop/Accelergy、MAESTRO/MAERI，还有 AutoSA、TensorLib 这类面向脉动阵列的综合工具。

这类工具的建模粒度通常更细，它们关注的是单个处理单元、本地缓存、甚至寄存器层面的映射，目标是设计或者综合出专用的加速器结构。它们更偏向架构探索工具，而不是面向现有硬件的产品级编译器。

TileLoom 的粒度则粗很多，它假设每个核心内部的结构和映射是固定的，把单个核心当作原子单位，只关注核间的空间分布、数据流动和存储层次调度。它的目标是给已有的空间数据流芯片提供通用的编译支持，而不是设计新的加速器。

简单来说，协同设计工具是造芯片的时候用的，TileLoom 是有了芯片之后写程序用的，两者处在不同的层级上。

### 8.2 与空间架构编译映射工作的差异

> **第二类相关工作是面向空间架构的软件映射与编译器** ，比如 CGRA 领域的 DSAGEN、Morpher，还有针对特定空间架构的系统级编译器。

传统的 CGRA 编译器通常做的是指令级或者操作级的映射，把数据流图里的每个节点放到不同的 PE 上，再做布线和调度，粒度非常细，适合细粒度的 CGRA 架构。

而 TileLoom 做的是 tile 级的映射，每个核心执行完整的 tile 计算，调度的单位是整个 tile ，粒度要粗得多，对应的是 Tenstorrent 这类粗粒度的空间数据流架构。

还有一些针对特定空间架构的系统级编译器，它们通常是厂商自己做的，里面硬编码了很多针对特定硬件的启发式规则，换一款芯片就不能用了。TileLoom 则通过统一的硬件表示实现了架构无关性，通用性要好很多。

另外还有很多工作停留在设计空间探索的层面，只能分析评估映射的性能，不能生成实际可运行的代码。TileLoom 是完整的端到端编译栈，可以直接生成可执行程序，在真实硬件上运行，这也是非常重要的区别。

**TileLoom 在现有研究版图中填补了粗粒度空间数据流架构通用端到端编译的空白** 。它以 tile 为基本调度单位，聚焦核间数据流规划，兼顾通用性与实用性，将空间架构的编译从手工定制的模式推向了自动化、通用化的新阶段。

## 九、总结与展望

> TileLoom 这项工作，为空间数据流架构的可编程性问题提供了一套完整且经过实际验证的解决方案。 它的意义不止于一个具体的编译工具，更在于证明了自动化的数据流规划完全可以达到手工优化的性能水平。

### 9.1 核心贡献与产业价值

总结下来，TileLoom 的核心贡献主要有三个方面。

| 序号 | 内容要点 | 详细说明 |
| --- | --- | --- |
| 第一 | 完整的 tile 级编译方法论 | 提出融合时空映射、数据复用规划、硬件抽象与性能建模的可复用编译流水线，为研究与工程实现提供清晰参考框架。 |
| 第二 | 基于 MLIR 的 df 硬件表示方言 | 实现编译器优化逻辑与硬件参数解耦，降低多硬件适配成本，提供统一软硬件协同评估接口，推动空间架构生态建设。 |
| 第三 | 真实硬件验证自动化方案竞争力 | 在多个核心算子上，通用自动编译方案性能达甚至超厂商手工优化库；在访存密集型及厂商未融合的新算子上优势显著，证实空间架构可编程性可通过编译技术解决。 |

对于产业来说，TileLoom 这类技术的成熟，会大大降低空间数据流芯片的软件生态建设门槛。开发者不需要学习复杂的底层编程，用 Triton 这类已经比较普及的高级语言写的算子，就能自动映射到空间架构上高效运行。这会加速空间数据流架构的商业化落地，也会推动 AI 加速芯片的架构多元化发展。

### 9.2 局限与未来方向

> 当然，TileLoom 目前也还有一些局限和可以继续拓展的方向。

- 首先，目前的映射策略都是基于规整分块的， **对于稀疏计算、不规则访存的算子支持还不够** 。稀疏计算、不规则访存是空间架构重要的潜在应用场景，相应的编译技术还需要进一步发展。
- 其次， **目前的设计空间搜索还是枚举式的，对于更大规模的核心阵列，搜索成本会上升** 。未来可以引入更智能的搜索算法，比如基于强化学习或者遗传算法的搜索，在更大的设计空间里高效找到最优解。
- 另外，目前的工作主要关注单个算子的优化， **对于多算子的整图调度还没有涉及** 。实际的模型里算子是串联的，中间结果的布局、算子之间的数据传递也有很大的优化空间。未来可以把数据流规划扩展到整图层面，实现端到端的模型级优化。
- 最后， **目前只在 Tenstorrent 的硬件上做了验证** ，未来可以拓展到更多的空间数据流架构上，比如 Cerebras、Graphcore 的产品，进一步验证框架的通用性。

总的来说，TileLoom 是空间数据流编译领域非常扎实的一步，它把这个方向从零散的手工方案带到了系统化自动化的阶段。随着空间架构的不断发展，相信这类编译技术会越来越成熟，最终让空间数据流架构的算力红利真正释放给每一个开发者。

**相关推荐**

- **[GEMM/FA 平均性能超厂商库 1.03×/1.91×！面向空间数据流架构的端到端 Tile-Based 编译器 TL](https://mp.weixin.qq.com/s?__biz=MjM5NDczOTA4NQ==&mid=2447896903&idx=1&sn=78559ef90da4c8638475faac4265f65a&scene=21#wechat_redirect)**
- **[碾压GH200注意力实现：FlatAttention在Tile数据流加速器上，计算场景利用率达86%，访存场景带宽利用率达 78%，注意力内核平均加速 1.9倍](https://mp.weixin.qq.com/s?__biz=MjM5NDczOTA4NQ==&mid=2447899603&idx=1&sn=59da0ff7b8208a308553b463fc36a817&scene=21#wechat_redirect)**
- **[解锁下一代 AI 加速器性能潜力！普渡、Meta提出 AI 加速器复合操作数据流和显式集体通信建模方案 COMET](https://mp.weixin.qq.com/s?__biz=MjM5NDczOTA4NQ==&mid=2447896642&idx=1&sn=ed9b64a8a02e6db7a61a486e6fe64d65&scene=21#wechat_redirect)**

**交流加群请在 NeuralTalk 公众号后台回复：加群**

嵌入式AI · 目录