# 2026-08-01

## SDSoW→iNEST 学术汇报 PPTX 生成（已完成）
- 交付物：`D:\Obsidian\vault\50_Output\SDSoW_iNEST_PPT\介观尺度计算新范式.pptx`（20 页，学术风 深蓝/白/浅蓝，1280×720）。
- 技术路径：tencent-pptx 的 slidep 引擎（@tencent/slidep 5.4.1）在受管 node 下 npm 安装被沙箱掐断（canvas@3 NAPI-v7 预编译不匹配 + 可选依赖卡死），故改用 python-pptx 1.0.2 + matplotlib(mathtext) 兜底生成。`build_pptx.py` 为生成脚本，`validate.py` 为校验脚本。
- 已落地的三处关键修正：
  1. 封面明确报告人「邬江兴 院士」。
  2. R_I 判据统一为三档：`<1 难以胜任` / `≈1 能力匹配` / `>1 游刃有余`；删除原「远大于1进入候选区」措辞（修订注释也不再出现该旧词）。
  3. `A(t^+)=Π_SDI[...]` 明确为「SDI 拓扑调控算子」——从「受控 SDDE 混合动力系统」抽象出的**工程控制律**，并声明并非自然定律/普适定律。
- 公式（CST V4.0、R_I、SDDE、SDI 算子、Route≈Transform、六等级阶梯等，共 24 张 mathtext PNG）已嵌入。
- 备注：`pages/slide_*.jsx`(20 个) 是按 slidep DSL 写的「预期格式」，因引擎不可用目前不编译；python-pptx 的 .pptx 是可用交付物。
