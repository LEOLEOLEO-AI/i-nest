# 2026-08-04 工作日志

## 知识库每日自进化（automation-1785546922604）
- 运行 `self_evolve.py`，耗时约 2 分钟
- **compile**：超时（wiki_compiler 300s 未返回，raw/ 积压较大）
- **wiki_grow**：概念 485，已链接 472，孤儿 242
- **健康自检**：笔记 5141 · 断链 2441 · 孤儿 2505 · 缺 FM 355
- **概念补全**：新建 10 篇占位概念（智能涌现/晶上自演化/晶圆级神经网络/数字孪生大脑/SDSoW 等）
- **phase4**：4/4 成功（import/task/evolution/cross-domain 均绿）
- **homepage**：Home.md 生成 5324 chars
- **git**：提交 564 文件，push 首次失败（远端有 arxiv-auto 新提交），pull 后重推成功

## wiki_compiler 手动补跑（积压清理）
- 734 篇积压文件全部编译完成（raw/23 + 00_Inbox/144 + 20_Processing/567）
- DeepSeek LLM 抽取 2595 个概念，耗时 59m34s
- 1 篇 LLM 超时被优雅跳过
- git 提交 3352 文件（+66074/-1240），push github main 一次成功
- 积压已清零，下轮自进化应恢复正常增量模式

## 迭代与诊断修订（下午）
- **self_evolve.py 三处修订**：
  1. compile 超时 300s → 3600s（大积压不再误超时）
  2. `git add -A` → 精确目录列表（避免吞入 AI 工具残留）
  3. git push 增加 non-fast-forward 自动恢复（pull --no-rebase 后重推）
- **camelCase 别名批量修复**：
  - 诊断发现系统性断链：wiki_compiler 用 underscore 命名（`Memristor_Crossbar_Array`），文章用 camelCase 引用（`MemristorCrossbarArray`）
  - 新建 `fix_camelcase_aliases.py`，扫描 3005 个概念文件，1258 个添加了 camelCase 别名
  - wiki_grow 同步去重了 camelCase 重复概念文件（约 70 个被删除合并）
- **断链分类诊断**：
  - other: 2241（含 GammaSt_Revision 58次、SmallWorldNetwork 43次等高频缺失）
  - GetNote_long: 561（源文件间互引的长文件名，非真实断链）
  - date_pattern: 42（日记页交叉引用）
- **wiki_grow 性能瓶颈**：O(n²) co-occurrence 链接（3000 概念 → 450 万对），需改为倒排索引优化
- git 提交 1341 文件（+5053/-1506），push 成功
- **待办**：wiki_grow.py 性能优化（倒排索引替代 O(n²) 双重循环）
- **wiki_grow 完成**：3005 概念，2974 已链接（98.9%），孤儿 2307，合并 20 个重复概念，耗时 31 分钟
- git 提交 3728 文件（+38268/-10613），push 成功

## 性能优化与脚本维护（傍晚）
- **wiki_grow.py 5x 提速**：
  - name-phrase matching: 循环内 `re.compile` → `main()` 预编译 5988 个正则 + `cname in source_text` 快速预筛选
  - shared-term linking: O(n²) 双重循环 → 倒排索引（inverted vocab→concepts）
  - incoming 计算: Step4 二次 O(n²) 扫描 → Step2-3 同步构建
  - **耗时: 30m53s → 6m21s（5x 提速）**
  - **链接覆盖率: 2974/3005 → 3001/3005（99.9%）**
  - **孤儿: 2307 → 1872**
- **health_repair.py 目录适配**：
  - EXCLUDE 更新为当前编号体系（移除 99_*/03_Topics，添加 80_Archive/50_Output）
  - moc_map: 00_MOC/10_Library/20_Ideas → 当前实际目录
  - archive 路径: `_archive/` → `80_Archive/`
  - MOC_TEMPLATES + TOP_STUBS 路径映射到当前体系
- **self_evolve.py 补充修复**：
  - wiki_grow timeout: 300s → 600s（匹配优化后实际耗时）
  - git add: `check=True` → 手动检查 returncode，部分目录无匹配时不阻断流程
  - `.gitignore`: 添加 `_diag_*.py` / `_test_*.py` / `_tmp_*.py` 排除规则
- **验证运行**：self_evolve.py 全流水线通过（compile 17 篇/40 概念增量，health 8376 笔记，phase4 4/4，homepage 刷新）
- git 提交 3 次，push github main 成功
