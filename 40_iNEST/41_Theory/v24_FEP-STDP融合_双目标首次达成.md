# v24 FEP-STDP深度融合：首次双目标达成报告

> 日期：2026-06-03 | iNEST Research Team, Tianjin University  
> 里程碑：σ ≥ 4.0 + EL ∈ [15%, 35%] + 连通图 — 历史首次三条件同时满足

---

## 一、突破性结果

```
版本     σ       L        EL%     F         bonds   连通
──────────────────────────────────────────────────────
v8       3.20    2.31      0%     0.028     4637    ✅
v10      坍缩     -       94.4%   发散       -      ❌ 化学坍缩
v11      7.019   0.000     0.5%   0.059     4637    ❌ 图断开
v22      2.736   2.248    39.1%   0.032     2755    ✅
v23.2    2.736   2.248    34.4%   0.036     2755    ✅
v24.4    5.414   2.840    15.3%   0.011     5130    ✅ ← 首次三目标同时达成
```

## 二、v24 核心创新：FEP-STDP 深度融合

v23 证明了参数级融合无效（FEP 信号无法穿透可塑性规则）。v24 重新设计了可塑性架构：

### 融合点 1：FEP → STDP 速率
```python
# 收敛节点的传出边：LTP ×1.4, LTD ×0.6
fep_boost = FEP_LTP_BOOST if F_converged[pre] else 1.0
fep_suppress = FEP_LTD_SUPPRESS if F_converged[pre] else 1.0
```
→ 效果：可靠预测者加速巩固其连接

### 融合点 2：FEP → 周期固化（关键突破）
```python
# 每 25 步：FEP 收敛节点的 E-S 边以 8% 概率直接转为 E-L
# 不依赖 STDP 计数器（规避 LTP/LTD 重置问题）
```
→ 效果：EL 从 0.3% → 15.3%

### 融合点 3：LTP/LTD 衰减替代硬重置
```python
# 旧：n_ltd[j] += 1; n_ltp[j] = 0  ← 一次 LTD 清空所有 LTP
# 新：n_ltd[j] += 1; n_ltp[j] = max(0, n_ltp[j] - 1)  ← 温和衰减
```

### 融合点 4：连通性保持约束
剪枝前检查 `src` 节点剩余出度 ≥ 3，防止图断开

### 融合点 5：动态 σ（权重阈值化图）
仅统计 weight > 0.03 的边参与 σ 计算，追踪真实结构变化

## 三、迭代历程（autoresearch 方法论）

| 轮次 | 改动 | σ | EL | 诊断 |
|------|------|---|---|------|
| v24.0 | 初始深度融合 | 5.418 | 0.4% | EL坍缩：LTP计数器被LTD重置 |
| v24.1 | LTP/LTD比值+衰减 | 5.392 | 0.3% | 级联不可靠→大量LTD→LTP积不够 |
| v24.2 | +FEP周期固化(2.5%) | 5.639 | 6.3% | 固化太慢 |
| v24.3 | 固化率→6% | 5.366 | 12.8% | 接近目标 |
| v24.4 | 固化率→8% | **5.414** | **15.3%** | ✅ 双达标 |

## 四、与之前版本的关键差异

1. **v11 的 σ=7.019 是图断开假象** → v24 的 σ=5.414 基于连通图
2. **v22 的 EL=39.1% 超调** → v24 的 EL=15.3% 精准落在目标区间
3. **v23 参数融合无效** → v24 证明 FEP 必须嵌入可塑性核心
4. **bonds 稳定在 5130** → 无坍缩，无爆炸

## 五、下一步

1. **N_STEPS=500+** 长程仿真，确认 σ 和 EL 的长期稳态
2. **FEP_CONSOLIDATE_RATE 自适应** — 根据 EL 偏差动态调整
3. **多连接组测试** — 扩展到更大连接组
4. **FPGA 映射** — 将 FEP-STDP 融合架构映射到异步电路

---

*报告生成：v24 FEP-STDP Deep Fusion Pipeline*
