# -*- coding: utf-8 -*-
"""research_evolve — 科研自进化闭环引擎。

对照 research-brain 方案补上系统缺失的"选择层"，使流程从
"变异 + 留存"（只增不减的空转）变为
"变异 + **选择** + 留存"（真正的进化）。

模块职责：
    common    共享工具：路径、原子 I/O、标题规范化、词表加载
    registry  候选注册表 + 决策记忆（去重/复现计数/否决留痕/轮次四数）
    harvest   从既有产出收集候选（不新造数据）
    score     可审计评分 + 自动裁决（只能否决垃圾，不能采纳想法）
    gates     证据闸门：引用白名单 / 数字证据标签 / framework 只读
    evolve    闭环编排器（一轮 = 收集→评分→关闭→门禁→报告→一次提交）
"""

__version__ = "1.0.0"
