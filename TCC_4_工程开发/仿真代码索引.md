> [!info] 仿真代码迁移说明
> 本知识库中的仿真代码已执行物理隔离，统一迁移至本地外部工程文件夹：
> 👉 **`D:\iNEST\Write\Code`**
> 
> **为什么要分离？**
> 1. **避免 Git 灾难**：知识库以 Markdown 文本双向同步为主。如果将带有大体积 `ckpt` 权重、数据集和海量 `.pyc` 缓存的代码库放入，会直接撑爆 Gitee 仓库并导致同步瘫痪。
> 2. **保持搜索纯净**：分离后，Obsidian 全局搜索不会被杂乱的 Python 变量名污染，维持纯粹的“思考与输出区”定位。
> 3. **联合工作区**：在 Trae 中，使用 `文件 -> 将文件夹添加到工作区`，可以将 `Code` 文件夹和本 `workspace` 知识库在同一个 IDE 中并列加载，实现左手理论、右手代码的跨目录无缝协同。

### 核心仿真脚本锚点
- `01_SNNGrow_Liquid_Demo.py`：基于 SNNGrow 稀疏突触的液态拓扑首个生成式结构演示
- 后续即将开发的脚本：
  - `Kuramoto_Monte_Carlo.py`：用于验证不同器件传递函数 $\alpha$ 值的合成网络模拟
  - `Meta_Pruning_Analysis.py`：用于从文献提取数据反推真实 $\alpha_{CMOS}$ 值的分析脚本