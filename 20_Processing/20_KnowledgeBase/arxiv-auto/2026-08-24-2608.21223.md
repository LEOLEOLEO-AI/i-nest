---
title: "Event-triggered Implicit Perturbation for Zeroth-Order Fine-Tuning of Spiking Transformers"
arxiv_id: "2608.21223"
link: "https://arxiv.org/abs/2608.21223"
date_added: "2026-08-24"
tags:
  - arxiv-auto
  - literature
  - 2026-08
  - neuromorphic
---

# Event-triggered Implicit Perturbation for Zeroth-Order Fine-Tuning of Spiking Transformers

🔗 https://arxiv.org/abs/2608.21223

---

## 1. 核心创新

本文最重要的技术或理论突破是什么？与现有方案的核心区别？
verbatim: To address these challenges, we propose an implicit-perturbation ZO (IPZO) architecture in which perturbation sums computed by an event-triggered perturbation generation unit (PGU) are combined with the weighted sums produced by the IMC array, eliminating perturbation-induced RMW operations while preserving weight-stationary execution of IMC. By exploiting spike sparsity, the PGU generates and accumulates perturbation contributions only for spike-activated weight rows, reducing the required row dimension of the RNG array. An address-driven XOR recombination scheme (PGU-XOR) is further introduced to mitigate the spatial correlations caused by direct RNG reuse (PGU-Reuse).
answer: 本文的核心创新在于提出了隐式扰动零阶优化（IPZO）架构。与传统ZO优化中需显式修改权重（产生高额读-改-写RMW开销）的方案不同，该方案利用事件触发的扰动生成单元（PGU）将扰动和直接叠加在存内计算（IMC）阵列生成的加权和上，实现了权重不动（Weight-stationary）的执行模式。此外，利用脉冲稀疏性，PGU仅为激活的行生成扰动，并引入了基于地址驱动的XOR重组方案（PGU-XOR），有效解决了随机数生成器（RNG）重用带来的空间相关性问题，在保持精度的同时极大降低了硬件开销。

### Question:

## 2. 对 TCC / iNEST 的价值分析

对TCC、SDI、iNEST三个方向的价值或启发意义。
verbatim: IPZO architecture in which perturbation sums computed by an event-triggered perturbation generation unit (PGU) are combined with the weighted sums produced by the IMC array... by exploiting spike sparsity, the PGU generates and accumulates perturbation contributions only for spike-activated weight rows... (1) PGU-XOR matches software RNGs in accuracy on Spikingformer/CIFAR-10 (76.41% vs. 76.53%) and perplexity (PPL) on SpikeGPT/WikiText-2 (54.20 vs. 53.23).
answer: 1. **iNEST价值**：该研究直接支持“极简规则涌现复杂智能”。通过零阶优化（ZO）这一仅需前向传播评估的规则，在非微分SNN上实现复杂任务（如SpikeGPT）的微调，证明了局部/简单的扰动机制可驱动全局智能涌现。
2. **TCC价值**：论文利用“事件触发”和“脉冲稀疏性”进行计算优化，体现了拓扑中心计算中“按需激活”的特征，为自组织临界态的硬件实现提供了低功耗反馈机制的参考。
3. **SDI价值**：PGU-XOR重组方案展示了如何通过简单的逻辑重构（XOR）解决资源受限下的互连相关性问题，为软件定义硬件互连中的资源复用提供了算法级的启发。

### Question:

## 3. 后续应用方案建议

iNEST/TCC团队后续如何具体利用这篇论文？给出1-3条可操作建议。
verbatim: implemented in a TSMC 16-nm CMOS technology, PGU-XOR incurs 40.3%-46.0% area and 15.2%-48.9% energy overhead per matrix-vector multiplication relative to PGU-Reuse, yet its faster convergence reduces the total perturbation energy to 0.51x... IPZO reduces the perturbation energy to 0.46x-0.83x that of conventional explicit weight perturbation for a bat

---

## 原始摘要

> Zeroth-order (ZO) optimization estimates gradients using only forward-pass evaluations, making it suitable for fine-tuning non-differentiable, event-driven spiking neural networks (SNNs). However, its deployment on in-memory computing (IMC) accelerators is constrained by the repeated read-modify-write (RMW) operations arising from explicit weight perturbation and the prohibitive hardware footprint of random number generators (RNGs) for statistically independent per-weight perturbations. To address these challenges, we propose an implicit-perturbation ZO (IPZO) architecture in which perturbatio

---
*自动抓取于 2026-08-24 | iNEST arXiv WikiBot v2.2-gsk*
