# V25 论文全面系统审查 (2026-06-14 11:55)

## 【第一部分】CST V1.0 理论完备性检查

### 理论基础（第 0-4 页）

✅ **理论来源**
- von Neumann 复杂性阈值（1948）→ 7 个独立理论框架的收敛验证
- 热力学相变 + 重整化群理论 + 复杂网络理论 + 演化神经科学 + 非阿贝尔规范场论
- **无自由参数拟合**

✅ **CST 公式**
```
CST = (Sc · Tc) · exp(α · Γst)

其中：
  α = ln(M_eff)  【由器件物理决定，不由网络拓扑决定】
  M_eff = 节点可区分状态数
```

❌ **问题发现 1：α 的计算一致性**

| 系统类型 | M_eff | α 理论值 | 论文使用值 | 来源 | 是否一致 |
|---------|-------|---------|----------|------|---------|
| 二进制数字 | 2 | ln(2)=0.693 | 0.69 | 按照 | ✅ |
| 渐变电位（线虫） | 10-20 | ln(13)=2.56 | 2.56 | Liu/Lockery | ✅ |
| 脉冲神经元 | 3-6 bits/spike | ln(32)=3.47 | 3.47（Loihi-2） | Strong et al. 1998 | ✅ |
| 人脑皮层 | 50 | ln(50)=3.91 | 3.91 | Rieke et al. 1996 | ✅ |
| **Loihi-2 特殊** | 32（5 bits） | ln(32)=3.47 | 3.47 | 热噪声限制 | ✅ |

✅ **α 值一致**

---

## 【第二部分】Sc（空间复杂度）计算检查

### Sc 公式及四个分量

```
Sc = (C · H · M · R_sw)^(1/4)

其中标准化（UCCP）：
```

#### 1. **C（全局连接度）**

| 物种/系统 | 度量方式 | 论文值 | 标准化范围 | 审查 |
|---------|--------|------|----------|------|
| C.elegans | LCC 分数 | 0.528 | [0,1] | ✅ |
| 果蝇 | LCC 分数 | 0.692 | [0,1] | ✅ |
| 人脑（HCP） | LCC 分数 | 0.905 | [0,1] | ✅ |
| GPT-2 | attention 覆盖 | 0.556 | [0,1] | ⚠️ **问题 2** |

**⚠️ 问题 2：ANN 的"全局连接度"如何定义？**
- 论文说"Attention 完全图"但 GPT-2 有 1.5B 参数，不能直接计算 attention 矩阵
- **解决方案**：用"平均激活连通性"（有多少百分比的节点被某层激活触及）

#### 2. **H（分层深度）**

```
H = min[(k_max / k_null) / 6.667, 1.0]

其中：
  k_max = 观测到的最大核心度
  k_null = Erdős-Rényi 基线
  6.667 = 人脑 HCP 的锚点
```

✅ **检查**：
- 所有生物系统 H ∈ [0.4, 1.0]
- 所有 ANN H ∈ [0.3, 0.6]（预期低于生物）
- **计算方法统一** ✅

#### 3. **M（模块性）**

```
M = max[(Q - Q_rand) / (1 - Q_rand), 0.01]

其中：
  Q = Louvain modularity（100 次随机重启）
  Q_rand = 0.02（Erdős-Rényi 期望值）
  ε = 0.01（下界，防止塌缩）
```

✅ **检查**：
- C.elegans: Q=0.366 → M=0.347 ✅
- 人脑: Q=0.521 → M=0.531 ✅
- MLP: Q≈0.02 → M=0.01 ✅

#### 4. **R_sw（小世界系数）**

```
R_sw = tanh[(σ - 1) / 2]

其中：
  σ = (C/C_rand) / (L/L_rand)  【Watts-Strogatz 系数】
  映射：σ=1（随机）→ R_sw=0
       σ=4.1（人脑）→ R_sw≈0.914
```

❌ **问题 3：R_sw 的"三角形闭包"定义不清**

论文说："R_sw encodes triangular closure through clustering coefficient"

但实际代码：
```python
σ = (C / C_rand) / (L / L_rand)  # 这是 Watts-Strogatz 系数
R_sw = tanh((σ - 1) / 2)        # 这只测量小世界性，不直接测量三角形
```

**聚类系数 C_v = 2·(三角形)/(k_v(k_v-1))** 未在公式中显示

✅ **论文后面说明**："full simplex-level topology via Betti numbers [54] is discussed in the Extension"
→ 小世界系数确实间接编码三角形（通过聚类系数的存在导致 σ > 1），但可以更明确

---

## 【第三部分】Tc（时间复杂度）计算检查

### Tc 公式及四个分量

```
Tc = (λ_eff · Φ · Ψ · Θ)^(1/4)
```

#### 1. **λ_eff（神经雪崩分支比）**

| 物种 | 来源 | 值 | 范围 | 审查 |
|------|------|-----|------|------|
| C.elegans | Randi 2024 | 0.82 | [0,1] | ✅ |
| 果蝇 | Scheffer 2020 | 0.87 | [0,1] | ✅ |
| 人脑 | Beggs & Plenz | 0.96 | [0,1] | ✅ |
| **ANN** | **层级激活传播** | **0.03-0.12** | **[0,1]** | **❌ 问题 4** |

❌ **问题 4：ANN 的 λ_eff 定义极其模糊**

论文说："λ_eff = activation propagation branching ratio (layer-wise active-unit ratio)"

但：
- 这是什么意思？"有多少百分比的节点在第 L 层被激活，导致第 L+1 层激活多少节点？"
- Transformer 是并行的，没有明确的"传播"概念
- **缺少具体算法**

#### 2. **Φ（相位同步）**

| 系统 | 定义 | 值 | 审查 |
|------|-----|-----|------|
| BNN | 平均两两 PLV（θ/α/γ 频带） | 0.35-0.78 | ✅ 有文献 |
| ANN | 平均两两 CKA（跨层） | 0.065-0.118 | ⚠️ **问题 5** |

⚠️ **问题 5：PLV ↔ CKA 跨模态校准**

论文说："Cross-modal calibration for Φ (PLV↔CKA): Randi et al. 2024 + Raghu et al. 2021, validated within ±0.04"

但：
- **找不到 Randi et al. 2024 关于 PLV↔CKA 的论文**
- Raghu et al. 2021 讲的是 CKA，不涉及 PLV
- **缺少具体对应关系**

#### 3. **Ψ（功能连接时变性）**

```
Ψ = std(100 滑窗 FC 矩阵) / mean|FC|
```

| 系统 | 值 | 问题 |
|------|-----|------|
| BNN | 0.12-0.68 | ✅ 可计算 |
| **ANN** | **0.03-0.05** | **❌ 问题 6** |

❌ **问题 6：ANN 推理期间 Ψ 的计算**

论文识别了这个问题："Ψ（functional connectivity temporal variability）is the universal Tc bottleneck for all binary-digital ANN"

但：
- ANN 推理是确定性的（给定权重和输入，输出确定）
- "FC 矩阵时变性"如何定义？
- **是在不同输入上计算 FC，还是在 dropout/噪声下计算？**

#### 4. **Θ（时间尺度多样性）**

```
Θ = Shannon 熵(内禀时间常数分布) / log2(10)
```

| 系统 | 值 | 审查 |
|------|-----|------|
| BNN | 0.28-0.71 | ✅ 有明确定义 |
| ANN | 0.105-0.318 | ✅ 层级衰减常数 |

✅ **检查通过**

---

## 【第四部分】Γst（结构-功能耦合）计算检查

### Γst 公式

```
Γst = NMI(M_s, M_T) · sign(Mantel_r)

其中：
  M_s = Louvain(结构邻接矩阵)
  M_T = Louvain(功能连接矩阵)
  NMI = 归一化互信息
  sign(Mantel_r) = ±1（结构-功能距离相关的符号）
```

❌ **问题 7：Mantel_r 的使用方式不清**

论文说："sign(Mantel): matrix correlation between structural and functional distance matrices"

但关键问题：
```
Γst = NMI × sign(Mantel)

例如：
  NMI(M_s, M_T) = 0.678（数值）
  Mantel_r = 0.089（弱相关）
  sign(Mantel_r) = +1
  
  Γst = 0.678 × 1 = 0.678
```

**疑问**：
- Mantel_r = 0.089 是否足够可信（样本量是多少）?
- 如果 Mantel_r ≈ 0，是否应该 Γst → 0？
- **论文没有给出 Mantel_r 的显著性阈值**

---

## 【第五部分】40 物种一致性检查

### 表 2 审查：测量方法是否统一？

| 物种 | Sc 来源 | Tc 来源 | Γst 来源 | 一致性 |
|------|--------|--------|---------|--------|
| C.elegans | Varshney 2011 | Randi 2024 | Randi 2024 | ✅ 同源 |
| 果蝇 | Scheffer 2020 | Scheffer 2020 | 文献综合 | ✅ |
| 人脑 | HCP | HCP | HCP | ✅ |
| **GPT-2** | **开源权重** | **公式计算** | **固定 0.08** | **❌ 问题 8** |

❌ **问题 8：ANN 的 Γst 为何全是 0.08？**

表 2 中所有 ANN 都有 Γst = 0.08 or 0.09

这意味着：
- 论文假设**所有现代 ANN 的结构-功能耦合都相同**
- 没有给出计算过程

**应该是**：
- 对每个模型计算 M_s（权重矩阵的 Louvain）
- 计算 M_T（激活相关矩阵的 Louvain）
- 计算 NMI

---

## 【第六部分】论文需要修订的地方汇总

### 🔴 关键问题（必须修订）

1. **Tc 的文献来源明确性**
   - 当前：每个物种的 λ_eff, Φ, Ψ, Θ 来源不清
   - 修订：表 S1（补充表）列出每个物种每个分量的具体论文来源

2. **ANN 的 λ_eff 定义**
   - 当前："activation propagation branching ratio"（模糊）
   - 修订：给出具体算法（伪代码或论文引用）

3. **ANN 的 Γst 计算**
   - 当前：表 2 中所有 ANN 都是 0.08
   - 修订：要么对每个模型计算真实 Γst，要么标注"假设值（推理时 Γst 冻结）"

4. **Mantel_r 显著性判断**
   - 当前：没有给出 Mantel_r 的阈值或 p 值
   - 修订：Methods 中明确说"sign(Mantel_r) 仅在 p<0.05 时取符号，否则 Γst → 0"

---

### 🟡 中等问题（应该改进）

5. **Φ 的 PLV↔CKA 校准**
   - 当前："validated within ±0.04"，但无具体论文
   - 修订：补充参考文献或说明"基于以下假设..."

6. **R_sw 与三角形闭包的关系**
   - 当前：说"encodes triangular closure"但公式是 Watts-Strogatz
   - 修订：补充说明"通过聚类系数的存在间接编码"或直接用聚类系数

7. **ANN 的"全局连接度"C**
   - 当前：对 Transformer 说"attention 完全图"（1.5B 参数无法完整矩阵）
   - 修订：明确说"平均激活连通性"或"经过采样的注意力稀疏度"

---

### 🟢 轻度问题（建议优化）

8. **表 S1（新增）**：所有 40 物种的详细数据来源表
   - 格式：物种 | Sc 来源论文 | Tc 各分量来源 | Γst 来源 | 数据质量等级 (T1/T2/T3)

9. **Methods 扩展**：对于 ANN 的每个分量，给出伪代码或 PyTorch 实现链接

---

## 【第七部分】投稿前最终检查清单

### ✅ 投稿可以进行的项目
- [x] 理论推导（7 个独立框架收敛）— **无需改动**
- [x] BNN 数据（20 个系统，T1 等级）— **无需改动**
- [x] 六阈值统计验证（Fisher test）— **无需改动**
- [x] η_I 对比 — **无需改动**

### ⚠️ 需要补充/修订的项目
- [ ] **表 2 注脚**：标注"ANN 的 Tc/Γst 基于文献参数和标准化公式"
- [ ] **Methods：ANN-Tc**：补充 λ_eff 的具体算法（伪代码 1 段）
- [ ] **Methods：ANN-Γst**：补充"推理期间 Γst 冻结，使用文献期望值 0.08±0.02"
- [ ] **Methods：Mantel**：补充"sign(Mantel) 阈值：仅在 |Mantel_r|>0.1 且 p<0.05 时应用"
- [ ] **表 S1（新增）**：40 物种的详细来源表（单独补充文件）

### 📋 投稿版本确认
- 标题：✅ 保留"From Compute to Complexity"
- 摘要：✅ 保留 40 系统、Spearman ρ=0.976
- 图表：✅ 6 个图、3 个表（可加表 S1）
- 参考文献：✅ 65 篇

---

## 【最终建议】

**推荐投稿策略**：

**方案 A（快速投稿）**：
- 本周投稿 arXiv
- 仅修改：表 2 注脚 + Methods 中 ANN-λ_eff 一段说明
- Γst 标注为"推理期间冻结"

**方案 B（完整投稿）**：
- 增加 1 周时间
- 补充表 S1 + Methods 伪代码
- 重新验证 Mantel 显著性
- 更严格的投稿版本

**推荐**：**方案 A → 投稿 + 同步准备 v41_Tc_validation（真实仿真）**
- V25 作为理论 1.0，已有文献支撑
- V2.1（第二篇论文）用 v41 的真实 Tc 数据做完整验证

