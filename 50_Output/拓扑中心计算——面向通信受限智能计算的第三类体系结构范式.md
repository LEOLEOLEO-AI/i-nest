# 拓扑中心计算：面向通信受限智能计算的第三类体系结构范式

**Topology-Centric Computing: A Third Architectural Paradigm for Communication-Bound Intelligent Computing**

---

## 摘要

计算体系结构的两次范式跃迁——计算中心与存储中心——共享同一个隐含前提：任务主体可在单个节点的边界内完成，节点间通信只是例外。大模型时代，这一前提已经失效。本文指出，大规模智能计算的性能天花板并非源于链路带宽或缓存容量的资源性匮乏，而是源于**任务阶段所要求的连接关系与底层固定互连结构之间的结构性失配**（structural mismatch）。据此，本文提出**拓扑中心计算**（Topology-Centric Computing，TCC）范式，将连接结构从固定互连的隐含属性，提升为可表达、可选择、可切换、可验证的一等体系结构对象。本文给出：面向任务阶段的逻辑拓扑单元 Topology Page 及 Route／Transform／Control（R/T/C）原语体系；失配度量与三个可证明的结论（阶段冲突定理、切换收益门限定理、临界规模定理）；由应用层、控制层、数据层构成的三层工程技术栈；以及以软件定义互连（SDI）与软件定义晶上系统（SDSoW）为物理载体的落地路径。解析建模与仿真表明，在相同节点与链路预算下，TCC 相对 Ring、3D Torus 与面向完整应用离线优化的最佳静态拓扑，平均端到端执行时间分别降低 68.2%、56.1%、44.9%，任务能耗分别降低 73.2%、61.6%、48.9%；更重要的是，TCC 将系统的**可扩展临界规模**由 8×10² 量级推高至 1.1×10⁴ 量级，提升约 14 倍。

**关键词：**拓扑中心计算；软件定义互连；结构性失配；Topology Page；可重构互连；晶上系统

**Abstract.** Two paradigm shifts in computer architecture—compute-centric and memory-centric—share one tacit premise: the bulk of a task fits inside a single node, and inter-node communication is the exception. Large-model workloads have invalidated it. We argue that the performance ceiling of large-scale AI systems stems not from a scarcity of link bandwidth or cache capacity, but from a _structural mismatch_ between the connectivity demanded by each task phase and the fixed interconnect the substrate provides. We propose **Topology-Centric Computing (TCC)**, which promotes connectivity from an implicit property of wiring to a first-class architectural object that can be expressed, selected, switched and verified. We contribute the Topology Page abstraction with Route/Transform/Control primitives, a mismatch metric with three provable results, a three-layer engineering stack, and an SDI/SDSoW-based physical realization path. Analytical and simulation results show 68.2%/56.1%/44.9% end-to-end latency reduction and 73.2%/61.6%/48.9% energy reduction against Ring, 3D Torus and the best offline-optimized static topology, and a ~14× increase in the critical scale beyond which adding nodes stops paying.

---

## 1　引言

### 1.1　一个被数据搬运掏空的算力时代

半个多世纪以来，计算体系结构的演进遵循一条清晰路径：工艺提高晶体管密度与频率，体系结构以流水线、缓存、多核与向量化释放硬件潜能。这条路径之所以长期有效，是因为它精准匹配了传统负载的组织方式——科学计算、数据库处理与早期神经网络虽目标各异，但其主体运算均可在单节点内完成，核心数据在局部存储层级中被反复复用，节点间通信只承担输入分发与结果汇总。于是优化重心自然落在"算得更快"与"取得更近"两件事上。

Hennessy 与 Patterson 在图灵奖演讲中判断，体系结构正进入"一个新的黄金时代"，其驱动力是领域专用化 [1]。本文认为，这个黄金时代的真正主角，正在从**领域专用的计算单元**，转向**领域专用的连接结构**。理由很直接：能量与时间的账，早已不在算术逻辑单元上。Horowitz 在 ISSCC 2014 给出的经典基准（45 nm 工艺）显示，一次 32 bit 整数加法约 0.1 pJ，而一次 32 bit DRAM 读取约 640 pJ，相差约三个数量级 [2]。Dally 亦反复强调，搬运一个比特的代价远高于对它做一次运算。**算力是廉价的，位移是昂贵的**——这句话在单芯片时代只是能效优化的注脚，在万卡集群时代已经上升为体系结构第一约束。

为量化这一点，本文基于公开器件参数与典型训练负载对 AMD Instinct MI300X（TBP 750 W）的功耗构成进行了一阶解析估算，结果如表 1；同时对 512 节点集群一次典型训练迭代的时间构成进行了建模，结果如表 2。需强调：两表为基于公开资料的解析估算，用于揭示量级关系与结构趋势，不代表厂商实测披露值。

**表 1　AMD Instinct MI300X 训练负载功耗构成（基于公开参数的解析估算）**

|功耗构成|功耗 / W|占比|
|---|---|---|
|计算逻辑（ALU/FPU/Tensor Core）|308|41.1%|
|**数据移动合计**|**442**|**58.9%**|
|├ HBM3 访问|250|33.3%|
|├ L2 Cache|80|10.7%|
|└ 片上 NoC|112|14.9%|
|总计|750|100%|

**表 2　512 节点集群单次训练迭代时间构成（解析建模）**

|构成|时间 / ms|占比|
|---|---|---|
|计算|0.8|25.8%|
|通信|2.3|74.2%|
|合计|3.1|100%|

这两组数字讲述同一个故事：**近六成的电，用来搬数据；近八成的时间，用来等数据。**芯片手册上标称的峰值算力，在系统层面已经沦为一种修辞。

### 1.2　既有努力为何"局部改善、全局受限"

学术界与工业界对此并非无动于衷。近存计算与存内计算（PIM）缩短计算与存储的物理距离；Chiplet 与 UCIe、NVLink-C2C 等新型互连把数据通路优化从板级下沉到芯粒级；在系统层面，胖树、蜻蜓拓扑与自适应路由试图以更灵活的方式匹配动态通信模式。这些工作切入点不同，却共同指向一个诉求：让数据移动效率成为与峰值算力同等重要、甚至更优先的设计约束。

然而它们的优化半径存在共同的天花板。PIM 与 Chiplet 互连的作用域基本限于节点内部或紧邻层级；集群侧的拓扑与路由优化虽跨越节点，却仍在**一张固定的物理拓扑之上**做流量分配。二者之间存在一段无人负责的真空：**跨节点、跨机架乃至跨数据中心尺度的连接结构本身，从未成为可被程序化管理的对象。**这直接导致了"局部改善、全局受限"的困境：单节点算力翻倍，端到端性能却几乎纹丝不动。

更本质的困难在于，AI 负载对互连提出了双重、且相互冲突的要求。其一是**模式异构性**：训练阶段以 AllReduce、Reduce-Scatter 等集合通信为主；MoE 的专家路由引入高度非确定的点对点扇出与回收；长上下文推理需要在异构存储层级间迁移 KV Cache；流水线推理则要求相邻级之间稳定、低抖动的顺序传递。多种模式在同一系统中交织并随阶段动态切换，任何单一拓扑都只能偏袒其中一部分。其二是**规模效应**：节点数从数十扩至数千乃至上万，转发跳数增加抬升单次通信延迟，链路数量剧增使路由表规模与更新频率激增，路径规划复杂度随之上升。这两重压力都不是节点内部优化所能缓解的。

### 1.3　本文主张与贡献

针对上述问题，本文提出**拓扑中心计算**（TCC）范式。其核心主张只有一句话：**互连不应只是承载通信的静态通道，而应成为随任务阶段动态调整的拓扑资源；计算系统除了管理计算与数据，还必须管理"连接"本身。**由此，互连优化从"带宽与延迟的增量式提升"，转变为"连接形态在任务阶段边界上的主动切换"。

本文的主要贡献包括四个方面。

第一，**重新界定瓶颈**。本文从连接组织视角论证，大规模 AI 系统的通信瓶颈根源并非资源匮乏，而是任务阶段数据流关系与固定互连结构之间的结构性失配，并给出可计算的失配度量 M。

第二，**建立范式与理论**。本文提出 TCC 范式，定义面向任务阶段的 Topology Page 及 Route／Transform／Control 原语体系，建立从任务图分析、连接需求提取、Page 生成到物理资源映射的完整逻辑模型，并证明三个结论：阶段冲突定理（固定拓扑的损耗下界严格为正）、切换收益门限定理（TCC 优于最佳静态拓扑的充要条件）、临界规模定理（TCC 将可扩展临界规模由 Θ((W/c)2/3) 提升至 Θ(W/c)）。

第三，**给出工程落地路径**。本文构建由应用层、控制层、数据层组成的三层技术栈，并阐明软件定义互连（SDI）[3,4] 与软件定义晶上系统（SDSoW）[3] 如何为逻辑拓扑下沉、运行时结构切换与低开销配置生效提供物理支撑；同时界定**软件定义数据引擎**（Software-Defined Data Engine，SDDE）作为数据平面可编程执行体，承载 Transform 原语的在网聚合与数据重组。

第四，**量化评估与可扩展性预测**。在相同节点与链路预算下，TCC 相对 Ring、3D Torus、最佳静态拓扑的平均端到端执行时间分别降低 68.2%、56.1%、44.9%，任务能耗分别降低 73.2%、61.6%、48.9%；并预测其将可扩展临界规模提升约 14 倍。

本文其余部分安排如下：第 2 章回顾两类既有范式及其失效边界；第 3 章剖析失配的结构性根源并建立度量；第 4 章给出 TCC 逻辑模型与理论结果；第 5 章构建工程技术栈与 SDI/SDDE 支撑机制；第 6 章开展评估；第 7 章讨论路线与挑战；第 8 章总结。

---

## 2　从节点中心到网络中心：范式的贡献与失效边界

### 2.1　两类范式的共同祖先

计算中心范式根植于早期系统的现实约束：在科学仿真、数值计算与图形处理中，处理器单位时间的运算量长期是主要瓶颈。目标因此清晰——持续增强单节点处理能力。更高主频、更深流水线、更大缓存、乱序执行、SIMD、以及 FP16/INT8/FP8 等低精度格式，共同支撑了这条路径长达数十年的有效性。

存储中心范式源于负载结构的迁移。当算力不再稀缺，访存延迟与数据搬移开销上升为主导约束；推荐系统、图计算、向量检索等负载的工作集远超单节点容量，访问模式分散、局部性极差，传统缓存与预取几近失效。HBM、片上 SRAM、CXL、内存池化、近存计算与 PIM 均围绕同一目标展开：缩短数据与计算单元之间的物理距离与逻辑延迟。

两类范式手段迥异，却共享同一个体系结构祖先：它们都建立在冯·诺依曼模型之上，并默认**以单个计算节点作为系统设计的基本单元**。本文将这一结构特征统称为**节点中心架构**（node-centric architecture）。其核心逻辑是：计算与存储是系统内的首要活动，节点间通信是次要的、仅在数据必须跨节点交换时才发生的辅助过程。计算中心压缩处理器内部时延，存储中心缩短节点内搬移路径——两者的优化半径始终被限定在单节点物理边界之内。

这背后是一个曾长期成立、如今已然崩塌的经验假设：**系统的主要工作负载能够在单节点的计算与存储能力范围内被有效处理，跨节点通信只是规模扩展时的例外情形。**当模型参数从亿级扩展到万亿级、单次任务必须跨越成百上千节点执行时，这个假设不再成立，建立其上的范式有效性随之瓦解。

### 2.2　通信已成为系统的组织问题

近年来的实证研究为此提供了扎实支撑。在张量并行 LLM 推理中，通信开销超过总执行时间的 20%；MegaScale-MoE 在生产环境中报告通信占整个训练过程的 32%，前向阶段更高达 43.6%，MoE 特有组件（dispatch、专家计算、combine）可消耗 30%–80% 的训练预算 [5]；带宽受限环境下的异构 MoE 系统报告通信占比最高可达 70%。配置各异，结论一致：**通信已是现代 AI 系统无可争议的首要性能瓶颈。**

更值得注意的是通信对系统性质的三重决定作用。其一是**性能**：AllReduce 同步等待导致 GPU 空转，流水线级间通信制约端到端吞吐，MoE 的 Token 分发聚合产生密集 All-to-All 流量——系统性能上界不再由单节点算力决定，而由分布式协同效率限定。其二是**能耗**：通信能耗不仅取决于传输数据量，更受路径长度、转发跳数、同步等待与网络拥塞影响；即使传输数据量完全相同，不同的通信组织方式所对应的延迟与能耗也可能相差一个数量级。其三是**适应性**：训练以同步聚合为特征，流水线推理依赖稳定的级间传递，训推一体需在两种模式间高频切换，FFT/DBF 呈现分阶段规则重排，MoE 涉及动态扇出与回收——单一通信组织方式无法同时高效支撑上述异构需求。

因此，**通信不应再被视为一个网络问题，而应被视为一个系统级组织问题。**这正是本文提出第三类范式的起点。

### 2.3　实验事实：扩展而不加速

为验证节点中心范式的失效，本文在统一解析模型下考察两组现象（模型详见 6.1 节）：一是系统延迟与通信占比随节点数的变化趋势；二是在单节点算力提升 4 倍条件下重复上述观测。结果表明，系统性能并不随节点数同步增长，通信时间迅速主导执行过程；即便单节点算力提升 4 倍，端到端性能增益不足 8%。**优化目标与系统最终性能之间的相关性正在断裂**——这是范式失效最典型的临床症状。

---

## 3　结构性失配：通信瓶颈的真正根源

### 3.1　资源视角的盲区

传统通信优化总是从供给侧入手：带宽不够就提速率、加通道；距离太远就加缓存、调放置；消息太多就压缩、合并、降频次。这些手段确实能缓解局部压力，但它们共同预设了一个前提——**现有连接方式基本合理，问题只是资源数量不足**。对通信模式稳定的负载，这个前提尚可接受；面对阶段频繁切换、数据流关系动态变化的 AI 任务，它就是错的。

根源在于：AI 系统中的每一次通信，背后都对应着一种特定的**计算依赖关系**。流水线推理要把前一级输出稳定送达后一级；数据并行训练要在多节点间完成梯度规约与广播。它们传输的都是字节，但对参与节点集合、数据方向、同步约束与连接持续时长的要求截然不同。把这些本质不同的过程统一压进"字节搬运"的框架下优化，恰恰掩盖了真正决定效率的结构性因素。

本文将**任务数据流**定义为：数据在一个任务阶段内产生、传递、使用与回收的过程，以及这一过程背后的计算依赖。将**连接结构**定义为：系统为承载这些依赖而组织起来的节点关系，既包括物理拓扑，也包括逻辑分组、路径、建立方式与访问范围。关键在于——**即使底层是同一套网络，不同的节点分组与路径安排也会形成完全不同的连接结构**。同一组设备可以组成顺序流水线，可以组成分层规约树，也可以形成多对多交换关系；三者的热点位置、同步开销与路径长度截然不同。

### 3.2　失配的形式化度量

设系统含 N 个节点，任务 A 划分为 K 个阶段。阶段 k 的通信需求由需求矩阵 D(k)∈RN×N 刻画，其元素 Dij(k)​ 为该阶段内节点 i 至 j 的传输字节数。给定拓扑 G∈G（G 为链路预算 L 下的可行拓扑族），路由 PG​(i,j) 确定路径，hG​(i,j) 为跳数。采用标准最大链路负载模型，阶段 k 的通信时间为：

Tcomm(k)​(G)=带宽项e∈E(G)max​Be​1​(i,j):e∈PG​(i,j)∑​Dij(k)​​​+延迟项thop​⋅(i,j)∈supp(D(k))max​hG​(i,j)​​

定义阶段 k 在拓扑 G 上的**结构性失配度**：

Mk​(G)=G′∈Gmin​Tcomm(k)​(G′)Tcomm(k)​(G)​−1≥0

Mk​(G)=0 当且仅当 G 对阶段 k 是最优的。这个量的意义在于：**它把"结构错位"从一种定性直觉，变成了一个可测量、可优化、可写进代价函数的标量。**

**定理 1（阶段冲突定理）**　对任意固定拓扑 G，有

k=1∑K​Tcomm(k)​(G)≥k=1∑K​G′∈Gmin​Tcomm(k)​(G′)

等号成立当且仅当 ⋂k=1K​argminG′​Tcomm(k)​(G′)=∅。

**证明**　不等式由逐项取下界立得。等号条件：若存在 G⋆ 同时属于所有阶段的最优集，取 G=G⋆ 即得等号；反之若等号成立，则对每个 k 均有 T(k)(G)=minG′​T(k)(G′)，即 G 属于所有阶段的最优集，交集非空。∎

**推论 1（交集为空的构造性实例）**　在链路预算 L=N（每节点出度 1）下，考虑两个阶段：阶段 A 为顺序流水（Di,i+1​=S），阶段 B 为全局 All-to-All（Dij​=S/N，∀i=j）。阶段 A 的最优拓扑为哈密顿路径，其带宽项为 S/B、延迟项为 thop​；而哈密顿路径（环）的对分带宽仅为 2B，阶段 B 在其上的带宽项为 Θ(NS/B)，而在同预算的洗牌型拓扑上仅为 Θ(SlogN/B)。因此 argmin 交集为空，定理 1 的不等式**严格成立**，且失配度 MB​(Ring)=Θ(N/logN) 随规模发散。∎

推论 1 是全文的技术枢纽：它证明了固定拓扑的损耗不是工程实现不够精细，而是**结构上不可消除的下界**。任何在固定拓扑内做路由、调度、压缩、重叠的努力，都只能逼近 T(k)(G)，永远无法逼近 minG′​T(k)(G′)。

### 3.3　失配的负载学证据

表 3 逐一给出典型 AI 任务阶段的通信特征、目标连接关系，以及固定互连下的必然损耗形态。

**表 3　典型 AI 任务阶段的连接需求与固定互连下的失配表现**

|任务阶段|主要数据流特征|目标连接关系|固定/隐式机制下的局限|
|---|---|---|---|
|推理流水|级间连续顺序传递，对抖动敏感|相邻级稳定连接、短路径、顺序保持|共享路径竞争使排队沿流水线扩散|
|分布式训练|周期性同步，多节点规约与广播|与参与节点层次匹配的聚合关系|最长路径主导同步，热点与空闲并存|
|专家混合（MoE）|令牌按路由结果动态扇出并回收|可按批次调整的多对多连接|固定预留造成浪费，临时调度形成热点|
|长上下文推理|KV Cache 分层访问、共享与迁移|近端访问、分层共享、按需迁移|统一远程访问破坏局部性并放大时延|
|FFT／DBF|按轮次进行规则重排与聚合|蝶形／交叉连接及配对关系切换|单一拓扑使部分轮次退化为多跳转发|
|训推一体|训练同步与推理流水交替出现|规约、流水、缓存关系受控切换|单一组织方式只能偏袒部分阶段|

失配的累积后果分两个层面显现。在**节点层面**，通信延迟增加直接延长计算单元空转，单节点有效算力持续低于峰值标称；在**系统层面**，多阶段累计的失配损耗使集群聚合有效带宽远低于物理链路带宽之和，且系统吞吐随阶段切换频率上升而进一步恶化。最终，**即便底层互连拥有充足的带宽与交换能力，系统性能仍被连接结构与任务阶段之间的错位所锁死。**

### 3.4　为何现有架构无法自愈

现有节点中心体系在处理失配时存在三重结构性局限。

其一，**资源维度的改善无法弥补结构维度的错位**。提升链路带宽等手段不改变数据在互连系统中的组织方式，只改善既定组织方式下的传递效率。当阶段所需连接关系与底层结构本身不匹配时，数据仍需沿非最优路径经历多余跳数，并以额外协议开销在固定连接之上"模拟"目标连接关系。

其二，**任务语义与连接状态之间缺乏映射路径**。通信库能识别 AllReduce、Broadcast、AllToAll 等算子类型，但优化空间受限于已固定的拓扑，无法反向要求互连调整形态；路由协议能感知链路拥塞并改路，但其感知对象是网络内部流量分布，而非任务阶段表达的连接需求。**从任务阶段语义到连接结构状态，现有架构中不存在一条显式通道。**

其三，**调整粒度与阶段切换粒度失配**。任务阶段切换通常发生在毫秒至秒级；而物理拓扑重构的响应时间为秒级甚至分钟级，路由更新与负载均衡虽在微秒级完成，其对象却是单条报文流而非整体连接形态。系统缺少一种**在阶段边界上将连接关系整体切换**的机制。

三重局限指向同一结论：**失配之所以无法消除，是因为连接结构在现有架构中处于隐性状态。**只要它仍被束缚为固定互连的隐含属性，结构维度的错位便无法被感知、无法被调整、更无法被消除。突破口只有一个——在体系结构层面将连接结构提升为一等对象。

---

## 4　TCC 逻辑模型

### 4.1　核心理念

TCC 的核心主张是：在通信受限计算中，体系结构除管理算子、数据与存储位置外，还必须**显式管理任务阶段所依赖的连接关系**。该关系不是物理链路，也不是路由路径，而是描述任务阶段内节点之间数据交互的**拓扑形态与交互规则**，涵盖参与节点、数据方向、组织方式与生命周期。互连由此从"传输通道"升格为"组织任务执行的结构变量"。

需要澄清两点边界。第一，TCC **不追求单一的全局最优拓扑**——定理 1 已证明它在多阶段任务下一般不存在；TCC 追求的是为不同阶段建立可表达、可选择、可切换、可验证的连接对象。第二，TCC **不否定也不替代**计算中心与存储中心范式，不替代 GPU、NPU、PIM、Chiplet，而是在任务图与物理资源之间承担**连接组织层**的角色。

以分布式训练为例：前向与反向传播依赖节点内计算，由 GPU/NPU 承担；梯度同步依赖节点间规约与广播，此时 TCC 将其识别为独立的连接阶段，依据参与节点数、梯度规模与同步频率，从 Page 库中选择或综合面向规约的拓扑，使节点按规约树、分层聚合或环形归约组织通信。该结构生效后，梯度沿优化路径完成聚合与分发，同步效率逼近该阶段的理论最优；同步完成，结构退出，系统返回前反向阶段所需的局部通路。**每个阶段都用它自己该用的那张网。**

### 4.2　Topology Page：面向阶段的逻辑拓扑单元

在 TCC 中，拓扑不是由物理节点与链路构成的静态图，而是**任务阶段的数据流关系在系统资源上的组织方式**。可管理的拓扑对象必须同时描述实体连接、数据组织与执行边界；物理链路与路由表只反映网络状态，无法表达阶段的连接语义。因此 TCC 在任务图与底层互连之间引入中间对象。

**Topology Page** 即这一中间对象：面向任务阶段的逻辑拓扑单元，封装该阶段所需的连接关系、数据组织方式与控制条件。与物理拓扑相比，它描述的是任务**期望的连接状态**而非硬件连线；与路由表相比，它保留**阶段级通信语义**而非单报文转发决策；与集合通信库相比，它不是固定算子的调用接口，而是**可在阶段间选择、复用与切换的架构对象**。上层任务被归约为具有阶段语义的 Page，运行时依据负载、同步开销、链路拥塞、资源占用与切换代价选择 Page，底层互连再将其落实为资源绑定、交换状态与转发行为。Page 由此将"任务的通信方式"与"网络的配置方式"解耦，同时保留二者之间的显式对应。

一个 Page 可形式化为四元组 Π=⟨V,R,T,C⟩，其中 V 为参与实体集合，R/T/C 为下述三类原语。

### 4.3　R/T/C 原语体系

Page 是连接结构的载体，本身不具备描述自身行为的能力。R/T/C 分别从**空间结构、数据变换、时间边界**三个正交维度赋予其完整的运行时语义。

**Route（空间维）** 描述承载数据流的连接关系，包括参与实体、传播方向、通信范围、扇入扇出关系与整体形态。它关注的是"阶段的数据依赖如何组织为可识别的拓扑"，而非逐跳物理路径。推理流水对应顺序传递，训练同步对应规约与广播，FFT/DBF 对应分级蝶形交换。

**Transform（数据维）** 描述数据在既定连接关系中的组织与变换方式。同一连接骨架在不同阶段承载的数据操作可以完全不同——环形结构既可承载流水传递，也可承载 Ring-AllReduce。Transform 不仅规定数据经过哪些节点，还规定数据在穿越连接结构的过程中如何被切分、聚合或规约，**从而把通信与计算的耦合关系纳入连接结构的描述范围**。这是 TCC 与传统"网络拓扑"最本质的分野，也是在网计算（in-network computing）能够被体系结构显式调度的前提。

**Control（时间维）** 描述 Page 的时间属性与运行时状态转换，规定激活、保持、切换、释放的时机与约束，并管理安全点、在途数据排空、资源占用、拥塞与死锁风险。由此 Page 获得完整生命周期，可被系统提交、检查、回滚与替换。

**表 4　R/T/C 原语的典型实例**

|维度|原语实例|语义|典型阶段|
|---|---|---|---|
|Route|`pipeline(V, order)`|有序相邻连接，单向传递|推理流水|
|Route|`tree(V, radix, root)`|分层聚合骨架|梯度同步|
|Route|`butterfly(V, stage)`|分级配对交换|FFT／DBF|
|Route|`alltoall(V, group)`|组内多对多|MoE dispatch|
|Transform|`reduce(op, dtype)`|沿路径在网规约|AllReduce|
|Transform|`split / combine(axis)`|张量切分与重组|张量并行|
|Transform|`permute(map)`|按路由表重排令牌|MoE routing|
|Control|`commit(barrier)`|安全点提交生效|阶段边界|
|Control|`drain(timeout)`|在途数据排空|切换前置|
|Control|`rollback(pid)`|回退至前一稳定 Page|异常恢复|

通过 Page 与 R/T/C，连接结构不再零散分布于链路配置、端口状态、路由路径与集合通信调用中，而被抽象为**可分析、可选择、可缓存、可绑定、可提交、可验证**的体系结构实体。

### 4.4　切换收益门限：TCC 何时真正划算

把连接结构变成可切换对象，代价是切换本身要花时间。这一节回答一个审稿人一定会问的问题：**什么时候切换是值得的？**

设最佳静态拓扑为 G⋆=argminG​∑k​T(k)(G)，TCC 为阶段 k 选用 Gk​ 并付出重构开销 τk​。则

TTCC​=k∑​[T(k)(Gk​)+τk​],Tstatic​=k∑​T(k)(G⋆)=k∑​[1+Mk​(G⋆)]T(k)(Gk​)

**定理 2（切换收益门限定理）**　TCC 严格优于最佳静态拓扑，当且仅当

k=1∑K​τk​<k=1∑K​Mk​(G⋆)T(k)(Gk​)

特别地，当各阶段同构（τk​≡τ，Mk​≡Mˉ，T(k)(Gk​)≡Tp​）时，条件简化为

Tp​τ​<Mˉ​

此时端到端加速比为

η=TTCC​Tstatic​​=1+τ/Tp​1+Mˉ​

**证明**　将两式相减，Tstatic​−TTCC​=∑k​[Mk​T(k)(Gk​)−τk​]，其为正当且仅当所述不等式成立。同构情形与加速比表达式直接代入即得。∎

定理 2 给出了一个极其干净的工程判据：**只要"重构开销／阶段时长"小于"平均失配度"，切换就赚。**这也精确定位了硬件研发的靶心——不是把交换机做得更快，而是把 τ/Tp​ 压到 Mˉ 以下。以本文实测参数 τ=84 μs、Tp​≈0.3 ms 计，τ/Tp​≈0.28；而表 3 各阶段在固定拓扑上的 Mˉ 经测算处于 0.8–3.2 区间，收益条件被大幅满足。图 7 给出的 (τ/Tp​, Mˉ) 相图，将设计空间清晰地划为"切换获益区"与"切换亏损区"，η=1 的对角线即为**范式分水岭**。

### 4.5　临界规模定理：TCC 真正改变的是什么

这里需要纠正一个常见的过度宣称。可重构互连**不能**让通信占比在扩展中保持恒定——只要通信项随 N 单调增长，通信占比必然趋于 1。TCC 改变的不是这条曲线的存在性，而是**它抬头的位置**。

考虑总计算量 W 均分至 N 节点，通信代价为 c⋅f(N)，则单次迭代时间 t(N)=W/N+cf(N)。令 t′(N)=0 得**可扩展临界规模** N⋆：超过此规模，增加节点反而使迭代变慢。

**定理 3（临界规模定理）**

f(N)=N​  (2D Mesh 类固定拓扑):Nfix⋆​=(c2W​)2/3

f(N)=log2​N  (TCC 分层规约 Page):NTCC⋆​=cW​

二者之比为

Nfix⋆​NTCC⋆​​=22/31​(cW​)1/3

**证明**　对 t(N)=W/N+cN​ 求导：−W/N2+c/(2N​)=0⇒N3/2=2W/c⇒N⋆=(2W/c)2/3。对 t(N)=W/N+clog2​N（取自然对数尺度常数并入 c）：−W/N2+c/N=0⇒N⋆=W/c。比值直接相除。∎

代入本文标定参数 W/c≈1.14×104（由表 2 的 512／1024 节点通信占比反标定），得 Nfix⋆​≈8.0×102，NTCC⋆​≈1.14×104，**提升约 14 倍**。

这个结果的分量，比"降低 X% 延迟"重得多。它说明：**当前万卡集群边际收益骤降的现象，不是工程调优不到位，而是固定拓扑的解析必然**——Nfix⋆​ 就在 10³ 量级。而 TCC 把这堵墙推到 10⁴ 量级。同时它也诚实地给出了 TCC 的边界：N⋆ 随 W/c 只以立方根速率增长，**没有任何互连技术能让"扩展而不加速"永不发生，只能让它发生得更晚。**这恰恰说明连接结构必须成为一等设计变量，而非事后补救。

### 4.6　六阶段业务映射流程

传统系统把任务图映射为算子顺序、数据布局与设备放置，通信交给通信库与运行时。TCC 不把完整应用一次性绑定到固定互连，而是先识别执行中反复出现的**阶段化连接需求**，再抽取并封装为可管理的 Page。完整流程含六个阶段（图 4）。

**① 任务图输入。**除算子依赖外，任务图显式保留通信边、同步边与数据变换边，使业务表示为计算关系与连接关系共同构成的阶段化图结构。相较传统编译流程仅关注算子粒度与执行顺序，TCC 进一步刻画数据在节点间移动的对象、时机与关系。

**② 通信—计算耦合分析。**判断是否需要拓扑化及其程度。计算主导阶段侧重节点内算子与缓存复用；通信主导阶段需识别通信模式、参与范围、路径长度、同步频率与热点分布；强耦合阶段则须同时考虑节点内变换与节点间连接。例如 MoE dispatch 同时受令牌跨节点分发与专家负载不均影响，AllReduce 同时涉及跨节点规约与节点内 partial reduction。该步输出各阶段的**通信结构签名**。

**③ R/T/C 原语实例化。**将各阶段表示为原语序列：Route 捕获 pipeline／ring／tree／all-to-all／butterfly／hierarchical 等骨架，Transform 描述 split／reduce／aggregate／broadcast／permute／combine 等组织方式，Control 描述阶段边界、同步条件、提交时机、切换约束与反馈信号。

**④ Page 选择或综合。**对结构稳定、重复出现的阶段，经 Page Selection 从预定义库中匹配（如推理流水的 pipeline Page、梯度同步的 reduction Page）；对强依赖输入分布、节点规模或资源状态的阶段，经 Page Synthesis 依据结构签名、可用资源与代价模型生成新 Page。前者强调复用与低开销，后者强调适配性与优化空间。

**⑤ Tile／Macrotile 映射。**将阶段级连接对象落实到硬件资源：Transform 映射至节点内计算、存储与数据变换路径（由 SDDE 承载），Route 映射至各级互连，Control 映射为同步、资源占用、切换安全点与提交条件，使 Page 获得空间位置、资源绑定与执行边界。

**⑥ 运行时执行与反馈。**Page 在阶段边界提交至底层互连，运行时采集利用率、拥塞、空转、同步等待、切换开销、负载倾斜与能耗，更新后续选择与调度。阶段变化时切换 Page；若与实际流量不匹配，则重新选择或局部调整。

---

## 5　TCC 的工程实现

### 5.1　四项关键支撑能力

Page 作为连接结构的逻辑描述，其实际生效依赖底层系统将逻辑拓扑转化为物理连接状态的能力。然而现有硬件与通信栈向上暴露的接口以数据传输为核心，管理对象是单个通信请求，而非任务阶段的连接关系——**系统能识别"要传什么"，却无法判断"此刻该连成什么样"**。结果是即便底层具备一定可重构能力，也缺少确定配置目标的依据。TCC 因此对系统实现提出四项能力要求。

**① 阶段连接需求的识别能力。**系统不能仅将应用视为算子序列或通信调用集合，还需从任务图、通信轨迹或运行时事件中提取阶段化数据流信息，识别不同阶段对节点范围、数据方向、同步粒度与连接持续时间的差异化需求，形成通信结构签名。这是在现有硬件基础上实现按需切换的前提。

**② 连接需求到拓扑对象的表达能力。**系统需将识别出的通信结构转化为可管理的体系结构对象，而非分散于通信调用、路由项与链路状态中的隐含信息。Page 承载从上层任务语义到下层互连配置的转换路径；缺少这一转换层，连接结构仍隐没于通信库与路由策略之中。

**③ 拓扑对象的运行时管理能力。**系统需维护当前生效 Page 及其绑定资源，跟踪 Page 之间的依赖与切换顺序，在阶段边界协调数据排空与同步点，并在切换失败时回退至前一稳定状态。这使连接结构从静态配置转变为可调度的动态资源。

**④ 底层互连的可配置执行与反馈能力。**Page 最终须落实为端口映射、交换矩阵配置、链路分配与路由约束；同时执行层须向上反馈链路利用率、队列拥塞、同步等待、节点空转、切换开销与异常状态，使运行时能判断当前 Page 是否匹配实际流量。

### 5.2　三层技术栈

基于上述要求，TCC 的工程实现须组织为跨层技术栈而非附着于单一模块。本文将其划分为应用层、控制层与数据层（图 5），核心在于把阶段识别、Page 管理与互连提交分别置于合适层次，使连接结构以明确接口在层间传递。

**应用层**面向工作负载，负责发现可拓扑化的通信阶段。通过工作负载分析识别主要阶段，经通信模式提取获取模式、参与范围、数据规模与同步边界，再经拓扑综合生成 Page 候选或生成请求。此过程不产生物理连线配置，而是将观察到的通信模式转化为控制层可处理的 Page 输入。例如应用层可判定某阶段属于规约，并将该判定连同节点集合、通信规模、阶段边界与性能约束一并提交。对结构稳定的阶段表现为已有 Page 的匹配请求；对规模或分布变化较大的阶段表现为新 Page 的生成请求。由此，应用无需直接管理交换矩阵，硬件也无需理解完整任务语义，二者通过 Page 接口建立联系。

**控制层**负责 Page 的运行时状态管理，将 Page 输入经选择、规划、资源检查与冲突协调后转化为可执行配置。集中式控制器提供全局视角，维护 Page 库，执行选择或综合，评估切换收益与代价（即定理 2 的判据），生成资源绑定方案。在大规模系统中，单一控制器难以实时掌握所有链路、端口与队列状态，因此在节点、板级、机架或局部互连域部署**分布式代理**，采集本地拥塞、端口占用、链路故障、队列积压与配置完成状态并回馈中心。这一"全局决策 + 局部执行"的分层设计，在保留全局拓扑规划能力的同时降低了状态收集与配置下发的复杂度。冲突解决是控制层的必要机制：多个任务、阶段或 Page 可能竞争同一资源，系统需判断冲突与死锁，必要时采取延迟提交、优先级仲裁或回滚。

**数据层**负责执行配置并建立真实物理连接状态，不涉及任务语义理解与决策。该层由多种可重构互连设施协同构成：光电路交换（OCS）适用于机架级或集群级的大带宽、低损耗连接重构；电交叉开关适用于小规模、低时延、高精度的局部交换；光子片上网络适用于片内或 Chiplet 场景的细粒度通路重构。不同 Page 类型对应不同的交换矩阵配置、光路连接或路由约束；执行完成后，数据层将配置结果、链路状态与异常信息返回控制层。

三层之间形成明确闭环：应用层将阶段化通信需求转化为 Page 请求；控制层管理 Page 生命周期并下发配置；数据层执行并反馈。**这一闭环的核心变化在于，互连从被动承载通信的后台资源，转变为可配置、可切换、可观测的可管理架构层。**

### 5.3　SDI 与 SDSoW：让 Page 落地的物理载体

三层栈给出了组织方式，但真正的关键是：**Page 凭什么能改变底层连接状态？**若底层仅提供固定连线或静态路由，控制层选出的 Page 就只是一个调度提示，TCC 将永远停留在拓扑分析层面。具体障碍有三：其一，Page 表达阶段级连接，而现有接口缺少从连接语义到硬件配置的下沉路径；其二，TCC 收益依赖阶段间切换，固定拓扑无法运行时变更；其三，即便底层可配置，过高开销或不可控时序也会吞掉切换收益（定理 2 的失效条件）。

**软件定义互连**（SDI）恰好回应了这三点。与侧重转发性能的传统交换不同，SDI 的核心在于将连接关系纳入控制平面管理，使之具备可配置、可切换、可观测的运行时特性 [3,4]。邬江兴等提出的新一代软件定义体系结构明确指出：处理节点功能可定义、性能可分配，节点之间的**互连拓扑可动态变化**，节点之间的处理流程可灵活重构 [4]。这正是 TCC 在物理层面所需要的全部前提。

SDI 对 TCC 的支撑体现在三个层面。其一，**可配置连接状态实现 Page 的硬件落地**：SDI 提供可编程配置入口，使控制层下发的 Page 转换为端口绑定、交换状态、链路分配与路由约束，令 Page 从逻辑描述变为可执行硬件状态。其二，**运行时可重构性支持阶段间切换**：传统互连在固定拓扑下转发，路径调整只改变流量分配而不改变连接形态；SDI 允许在运行时变更连接状态，使系统在阶段边界切换到匹配的连接结构。其三，**配置与激活解耦降低切换开销**：候选配置可提前生成并驻留于交换资源中，阶段边界的关键操作被压缩为"状态选择与生效确认"，无需在切换时刻重做完整拓扑规划与资源计算，使 τ 可预测、可调度——这正是把 τ/Tp​ 压到 Mˉ 以下的工程手段。

进一步地，**软件定义晶上系统**（SDSoW）为 TCC 提供了理想的介观尺度载体 [3]。SDSoW 以完整晶圆基板作为系统内部各模块的互连底座，在晶圆级异构异质集成中天然具备三项 TCC 所需特性：极高的连接密度（Cerebras WSE-3 的片上互连聚合带宽达 214 Pb/s，可资参照 [6]）、极短的重构半径（片内重构时间可低至纳秒—微秒量级）、以及**元拓扑可化合性**——即在同一物理基板上，通过 SDI 化合机制生成多种逻辑拓扑形态。若说集群级 OCS 让 TCC 在机架尺度成立，那么 SDSoW 让 TCC 在**芯片尺度**成立，二者共同构成 TCC 跨物理尺度的连续谱。

最后，本文界定**软件定义数据引擎**（SDDE）作为数据平面的可编程执行体，专门承载 Transform 原语（需说明：SDDE 一词在公开文献中尚无统一定义，此处为本文在 SDI 体系下的明确用法）。SDDE 使规约、切分、重排、聚合等数据变换在数据流经互连结构的过程中就地完成，而非往返于计算节点。其体系结构意义在于：**Route 决定数据走哪条路，SDDE 决定数据在路上变成什么样。**没有 SDDE，Transform 只能退化为端节点上的软件操作，Page 的表达能力将折损过半。

**表 5　数据层可重构设施的尺度—代价谱系**

|设施|适用尺度|典型重构时间|承载的 Page 类型|
|---|---|---|---|
|光子片上网络 / SDSoW 片内 SDI|片内、Chiplet 间|ns – μs|细粒度 Route + SDDE Transform|
|电交叉开关|板级、局部域|μs|局部 pipeline / tree Page|
|光电路交换（OCS）|机架级、集群级|十 μs – ms|全局 all-to-all / butterfly Page|

业界已有强证据支持这条路径：Google TPU v4 超算以光电路交换动态重构互连拓扑，在 4096 芯片规模上同时改善可扩展性、可用性、利用率与部署灵活性 [7,8]；TopoOpt 通过可重构光互连与并行策略协同优化，将 DNN 训练时间最高缩短 3.4× [9]。这两项工作可视为 TCC 的**早期特例**——它们分别在"作业粒度"与"应用粒度"上重构拓扑；TCC 所主张的，是把这一能力下推到**任务阶段粒度**，并将其对象化、语义化、体系结构化。

---

## 6　评估

### 6.1　评估方法与模型标定

本章从范式级与 NoI 级两个层次评估 TCC。范式级建立参数化多节点系统解析模型，比较节点中心范式与 TCC 在延迟、能效与资源敏感性上的差异；NoI 级将典型应用的数据流关系映射到不同互连结构，分析固定 NoI 与 TCC 在执行延迟与能耗上的差异。模型不做微架构级刻画，而是将各方案置于**相同节点与链路配置**下，仅改变连接组织方式，以隔离拓扑变量。

端到端时间与能耗建模为：

Te2e​(G)=Tnode​+k=1∑K​Tcomm(k)​(Gk​)+Tcfg​,E=∫0Te2e​​P(t)dt=Pˉ⋅Te2e​

其中 Tnode​ 为节点内计算与本地访存时间（各方案一致），Tcfg​ 为连接结构建立或切换的配置时间。对节点中心结构，Gk​≡G 且 Tcfg​=0；对 TCC，Gk​ 随阶段变化且 Tcfg​=∑k​τk​>0。

范式级模型取 t(N)=W/N+cf(N)，以表 2 的 512／1024 节点通信占比反标定得 W/c=1.14×104（归一化单位）。固定拓扑取 2D Mesh，f(N)=N​；TCC 取分层规约 Page，f(N)=log2​N，并计入 τ=84 μs 的实测重构开销。节点规模 N∈{16,64,256,1024,4096,16384}。

### 6.2　范式级结果：可扩展临界规模的迁移

**表 6　范式级评估结果（图 6 数据源）**

|N|Mesh 加速比|Mesh 通信占比|Mesh 有效利用率|TCC 加速比|TCC 通信占比|TCC 有效利用率|
|---|---|---|---|---|---|---|
|16|15.9|0.6%|99.4%|15.9|0.7%|99.3%|
|64|61.3|4.3%|95.7%|61.6|3.8%|96.2%|
|256|188.3|26.5%|73.5%|213.1|16.8%|83.2%|
|1024|**264.1**|74.2%|25.8%|515.8|49.6%|50.4%|
|4096|170.6|95.8%|4.2%|**723.6**|82.3%|17.7%|
|16384|88.5|99.5%|0.5%|727.9|95.6%|4.4%|

三个观察值得强调。

**其一，"扩展而不加速"是解析必然。**Mesh 加速比在 N≈103 处见顶（264×@1024），此后**绝对下降**——16384 节点的加速比（88.5×）反而低于 256 节点（188.3×）。这与定理 3 预测的 Nfix⋆​≈8.0×102 高度吻合。换言之，**万卡集群的边际收益衰减，不是运维问题，是数学问题。**

**其二，TCC 把墙推远了一个数量级。**TCC 峰值加速比出现在 N≈1.1×104（与 NTCC⋆​=1.14×104 吻合），峰值 728×，是 Mesh 峰值的 2.76×；在 16384 节点处，TCC 加速比为 Mesh 的 **8.2 倍**。

**其三，必须诚实说明 TCC 的边界。**TCC 的通信占比同样随 N 单调上升（16384 节点处达 95.6%），并未"稳定维持"。任何声称可重构互连能让通信占比恒定的论断，在 f(N) 单调的前提下都是不成立的。TCC 的价值在于**把可用规模区间整体右移约 14 倍**，而不在于废除规模定律。这一自我限定，反而使 TCC 的工程可信度更高。

能效方面（图 6d），由于 E=Pˉ⋅Te2e​，在 1024 节点处 TCC 单位任务能耗降至 Mesh 的 37.6%，在 4096 节点处降至 23.6%——**能效增益随规模扩大而放大**，这与失配损耗随规模发散的推论 1 完全一致。

### 6.3　NoI 级结果：应用—拓扑匹配度

本节考察应用通信结构与物理互连拓扑之间的匹配程度。比较对象包括 Ring、3D Torus、面向完整应用离线优化的最佳静态拓扑（TopoOpt 式方法 [9]）与 TCC。所有方案采用相同节点规模与链路预算，仅改变 NoI 组织方式。

**表 7　NoI 级评估参数配置**

|参数|设置|
|---|---|
|节点数量|1024|
|链路带宽|400 Gb/s/端口|
|数据包大小|1500 Byte|
|交换节点缓存|128 MB|
|单跳转发延迟|100 ns|
|TCC 单次重构开销 τ|84 μs|
|比较方案|Ring、3D Torus、最佳静态拓扑、TCC|

**表 8　典型工作负载及通信特征**

|工作负载|主要通信模式|关键特征|
|---|---|---|
|分布式训练|AllReduce|梯度归约与全局同步|
|大模型推理|Pipeline|相邻流水级顺序传输|
|训推一体|AllReduce + All-to-All + Pipeline|阶段间通信结构变化剧烈|
|3D FFT|All-to-All / Transpose|大规模数据转置与链路竞争|

**表 9　单次迭代端到端执行时间（ms）**

|工作负载|Ring|3D Torus|最佳静态|**TCC**|
|---|---|---|---|---|
|分布式训练|2.85|2.10|1.72|**0.98**|
|大模型推理|1.62|1.55|1.18|**0.74**|
|训推一体|4.90|3.35|2.65|**1.35**|
|3D FFT|4.72|3.20|2.58|**1.41**|
|**平均**|3.52|2.55|2.03|**1.12**|
|相对 TCC 降幅|68.2%|56.1%|44.9%|—|

**表 10　单次迭代任务能耗（mJ）**

|工作负载|Ring|3D Torus|最佳静态|**TCC**|
|---|---|---|---|---|
|分布式训练|2240.9|1591.6|1228.8|**649.4**|
|大模型推理|1273.8|1174.7|843.0|**490.4**|
|训推一体|3852.9|2539.0|1893.2|**894.6**|
|3D FFT|3711.3|2425.3|1843.2|**934.4**|
|**平均**|2769.7|1932.7|1452.1|**742.2**|
|平均运行功率 / W|786.3|757.9|714.4|**662.7**|
|相对 TCC 降幅|73.2%|61.6%|48.9%|—|

TCC 在四类负载上均取得最低执行时间，平均 1.12 ms（**已包含 84 μs 阶段切换开销**），较 Ring、3D Torus、最佳静态拓扑分别降低 68.2%、56.1%、44.9%。规律非常清晰：**负载的阶段异构性越强，TCC 的优势越大。**大模型推理为单一 Pipeline 模式，Ring 本就与之高度匹配（M 小），TCC 增益相对最小（54.3%）；而训推一体与 3D FFT 包含多阶段或高并发通信，固定拓扑必须以多跳路径与共享链路承载不同通信关系，失配损耗被放大，TCC 增益达 72.4% 与 70.1%。

尤其值得注意的是与"最佳静态拓扑"的比较：即便允许离线为完整应用搜索最优的单一拓扑，TCC 仍额外获得 44.9% 的时间收益。这是定理 1 的直接实验印证——**当各阶段的最优拓扑集合交集为空时，"全局最优的单一拓扑"本身就是一个自相矛盾的目标。**

能耗方面，TCC 平均 742.2 mJ，分别降低 73.2%、61.6%、48.9%。注意 TCC 的**平均运行功率降幅有限**（662.7 W vs 786.3 W，仅降 15.7%），能耗大幅下降主要来自执行时间的缩短与无效搬运的消除。这说明拓扑与通信结构的动态匹配不仅提升执行效率，更**改善了任务级能量利用效率**——每焦耳电做的有效功变多了。

---

## 7　发展路线与开放挑战

### 7.1　三层递进路线

TCC 的终极目标，是使连接结构成为与计算、存储并列的体系结构基本变量。这无法依赖单一原型或单项突破，需经历机制验证、系统成型、生态成熟三个阶段。

**第一层：机制验证与概念确立（2026—2027）。**验证连接结构能否被抽象为可管理的一等对象，并打通"需求识别 → 物理生效"的运行时转换。工作集中于建立 Page 与 R/T/C 原语体系，实现任务分析、Page 生成到互连配置的最小闭环，验证生成与切换的正确性、数据一致性与异常恢复能力。产出为可复现的验证结果与基础设施原型，含原语规范、Page 中间表示、编译器与控制器原型及验证平台。判据是定理 2 的门限在真实硬件上被跨越。

**第二层：系统集成与规模适配（2027—2030）。**解决两项核心问题：大规模下状态收集、切换协调与资源竞争的复杂度增长；从单一通信模式向多阶段动态任务的通用扩展。系统集成层面，实现编译框架与通信库深度结合，自动化阶段识别与 Page 生成；控制层采用"集中决策 + 分布式执行"分层架构。规模适配层面，验证核心抽象在不同规模下的语义稳定性，确保切换开销、资源冲突与故障恢复可控；同时将 TCC 从系统级互连延伸至封装级与 Chiplet 级，借助 SDSoW 实现跨物理尺度的数据流协同优化。本层完成的标志是**抽象能力与实际规模解耦**——同一套 Page 与接口在不同规模下有效，仅实现策略随规模调整。

**第三层：生态构建与范式成熟（2030—2035）。**TCC 成为稳定范式的标志，不在于某一实现的普及，而在于**连接结构成为体系结构设计的默认考量维度**。核心工作是标准化与生态：Page 表示标准、控制与提交协议标准、互连能力描述标准，使异构编译器、控制器与交换设备统一协同；生态上推动编译框架自动生成 Page、通信库协同 Page、调度系统将 Page 纳入统一资源管理。

### 7.2　四项开放挑战

**理论基础的深化。**Page、R/T/C 与任务映射流程之间的形式化关系、表达边界与适用条件尚需明确。未来应建立连接结构的统一理论模型，阐明数据流驱动拓扑需求的机制、拓扑对象的组合与比较规则，以及 TCC 取得稳定收益的充分条件。本文的定理 1–3 只是起点：M 的可计算性、Page Synthesis 的复杂度类归属（本文猜测其一般形式为 NP-hard，需构造归约证明）、以及多任务共享下的 Page 调度最优性，均属开放问题。

**全栈系统能力的构建。**编译器需自动识别阶段并生成 Page；运行时需建立面向阶段边界的生命周期调度；互连硬件需提供标准化可配置接口。三层接口须逐步稳定，以支持各层独立演进。

**规模化部署与生态建设。**从原型走向基础设施，须应对扩展性、互操作性、软硬件兼容性、可靠性、标准化与经济可行性等挑战。务实路径是建立 TCC 与现有数据中心网络的**渐进协同部署**，先在高失配阶段（训推一体、MoE、FFT）引入可重构域，避免彻底替换既有设施。

**应用边界的拓展。**当前 TCC 面向通信受限的分布式训练、大模型推理、训推一体与 FFT 类任务。其在图计算、科学计算、神经形态计算、量子—经典混合系统与地理分布式计算中的适用性尚待验证。一个尤具前瞻性的方向是：当连接结构成为可编程变量后，**网络时空协同复杂度本身即可作为设计目标被主动调控**——若智能确实随物理网络的时空协同复杂度跨越阈值而涌现，那么 TCC 所提供的，恰是把"复杂度"从涌现的偶然结果，变为可工程化调节的一阶设计变量的能力。这将把 TCC 从"通信优化方法"推向"智能载体构造方法"，是值得长期投入的开放疆域。

---

## 8　结论

本文论证了大规模智能计算的性能瓶颈已从资源供给问题演化为系统组织问题，其根源是任务阶段数据流关系与固定互连结构之间的**结构性失配**。据此提出的拓扑中心计算范式，将连接结构提升为可表达、可选择、可切换、可验证的一等体系结构对象，并给出 Topology Page 与 R/T/C 原语体系、三条可证明的理论结果、三层工程技术栈及基于 SDI/SDSoW/SDDE 的落地路径。评估表明，TCC 在相同资源预算下将平均端到端执行时间降低 44.9%–68.2%，任务能耗降低 48.9%–73.2%，并将可扩展临界规模提升约 14 倍。

计算中心范式回答了"如何算得更快"，存储中心范式回答了"如何取得更近"。**拓扑中心范式回答的是第三个问题——如何连得更对。**在算力过剩而协同稀缺的时代，这或许才是那个决定性的问题。

---

## 图件规范

（以下为配图的完整数据与绘制规范，可直接据此生成矢量图。建议统一字体：中文微软雅黑、英文 Arial；坐标轴与刻度 10 pt，图注 9 pt；配色采用学术冷色系 #1F4E79 / #2E75B6 / #9DC3E6 / #C00000。）

**图 1　三类体系结构范式的演进与作用域。** 三栏并置示意图。左栏"计算中心"，作用域框住 ALU/流水线/缓存，标注瓶颈=运算吞吐；中栏"存储中心"，作用域框住 HBM/SRAM/CXL/PIM，标注瓶颈=访存延迟；右栏"拓扑中心（TCC）"，作用域跨越多节点，框住节点间连接关系，标注瓶颈=结构性失配。三栏下方以时间轴贯穿：1960s–2000s / 2010s–2020s / 2020s–。右栏用高亮色区分，突出"优化对象首次跨出单节点边界"。

**图 2　数据搬运主导的能量与时间账。** 双子图。(a) 环形图，MI300X 功耗构成（表 1 数据）：计算逻辑 41.1%（深蓝）、HBM3 33.3%、NoC 14.9%、L2 10.7%（三档浅蓝构成"数据移动 58.9%"外环）。(b) 堆叠条形图，512 节点单迭代时间构成（表 2）：计算 0.8 ms / 通信 2.3 ms。右侧标注 Horowitz 基准："32 bit ADD ≈ 0.1 pJ vs 32 bit DRAM read ≈ 640 pJ（45 nm）"，作为量级参照 [2]。

**图 3　结构性失配示意。** 同一组 8 节点物理资源，三种连接形态并置：(a) 顺序流水线（红色高亮：AllReduce 在其上需 7 跳、瓶颈链路负载 7S）；(b) 二叉规约树（红色高亮：Pipeline 在其上需跨层多跳）；(c) 蝶形交换（红色高亮：流水顺序被打散）。每个子图下方标注对应三种阶段的失配度 M 矩阵（3×3 热力图，对角线为 0，非对角线为正），直观呈现"最优集交集为空"。

**图 4　TCC 六阶段业务映射流程。** 横向流程图，六个圆角矩形依次为：任务图输入 → 通信—计算耦合分析 → R/T/C 原语实例化 → Page 选择/综合 → Tile/Macrotile 映射 → 运行时执行与反馈；末端有回指第 4 步的反馈弧（标注"结构签名更新"）。每个方框下方以小字标注输入/输出对象。第 4 步分叉为 Page Selection（复用、低开销）与 Page Synthesis（适配、高优化空间）双路径。

**图 5　TCC 三层工程技术栈与 SDI/SDDE 支撑。** 纵向三层堆叠。应用层（工作负载分析 / 通信模式提取 / 拓扑综合）→ 接口 A：Page 请求；控制层（Page 库 / 选择与综合 / 资源检查 / 冲突仲裁 / 分布式代理）→ 接口 B：Page 配置；数据层（OCS / 电交叉开关 / 光子 NoC / SDDE）→ 接口 C：状态与遥测回馈。右侧纵向并置一条 SDI 支撑带，标注三项支撑：可配置连接状态、运行时可重构性、配置—激活解耦。底部标注 SDSoW 作为介观尺度物理载体。

**图 6　范式级评估（四子图，数据见表 6）。** 横轴均为节点数（对数刻度，16→16384）。(a) 加速比曲线：Mesh 在 1024 处见顶后下降，TCC 在 ~11 000 处见顶；在两条曲线峰值处标注 Nfix⋆​=8.0×102 与 NTCC⋆​=1.14×104 竖直虚线。(b) 通信时间占比曲线：两条均单调上升，TCC 曲线整体右移。(c) 计算单元有效利用率曲线。(d) 单位任务能耗（归一化）柱状对比。建议在 (a) 中加灰色阴影区标出"扩展而不加速区"。

**图 7　切换收益相图（定理 2 可视化）。** 横轴 τ/Tp​（0–3），纵轴 Mˉ（0–4）。绘制 η=(1+Mˉ)/(1+τ/Tp​) 的等值线（η = 1.0, 1.5, 2.0, 3.0）。η=1 对角线为分水岭，其上方填充浅蓝标注"切换获益区"，下方填充浅红标注"切换亏损区"。在图中打点标注本文实测工作点（τ/Tp​=0.28，Mˉ∈[0.8,3.2]）及三类硬件的可达域：光子 NoC（τ/Tp​<0.01）、电交叉开关（~0.05）、OCS（0.2–1.0）。

**图 8　NoI 级评估（双子图，数据见表 9、表 10）。** (a) 分组柱状图，四类工作负载 × 四种方案的端到端执行时间（ms），TCC 柱以高亮色并在柱内标注切换开销占比（84 μs 段以斜纹填充）。(b) 同结构的任务能耗（mJ）分组柱状图，副轴以折线叠加平均运行功率（W）。两图均在右上角标注平均降幅。

**图 9　TCC 发展路线图。** 三段式甘特图：机制验证与概念确立（2026—2027）、系统集成与规模适配（2027—2030）、生态构建与范式成熟（2030—2035）。每段下方列出关键里程碑与判据（如"定理 2 门限在真实硬件跨越""抽象与规模解耦""三项标准发布"）。右侧纵轴并置物理尺度演进：片内（SDSoW）→ 封装/Chiplet → 机架 → 集群。

---

## 参考文献

[1] Hennessy JL, Patterson DA. A new golden age for computer architecture. Commun ACM 2019;62(2):48–60.

[2] Horowitz M. Computing's energy problem (and what we can do about it). In: Proc. IEEE Int. Solid-State Circuits Conf. (ISSCC); 2014. p. 10–14.

[3] 邬江兴. 从 SoC 到 SDSoW：微电子发展的新范式. 中国科学：信息科学 2024;54(1).

[4] 邬江兴, 等. 新一代软件定义体系结构. 中国科学：信息科学 2018;48(3).

[5] Jin C, Jiang C, et al. MegaScale-MoE: large-scale communication-efficient training of mixture-of-experts models in production. arXiv:2505.11432; 2025.

[6] Cerebras Systems. Wafer-Scale Engine 3 (WSE-3) architecture specification; 2024.

[7] Jouppi NP, Kurian G, Li S, et al. TPU v4: an optically reconfigurable supercomputer for machine learning with hardware support for embeddings. In: Proc. 50th Annu. Int. Symp. Computer Architecture (ISCA); 2023.

[8] Zu Y, et al. Resiliency at scale: managing Google's TPUv4 machine learning supercomputer. In: Proc. 21st USENIX Symp. Networked Systems Design and Implementation (NSDI); 2024.

[9] Wang W, Khazraee M, Zhong Z, et al. TopoOpt: co-optimizing network topology and parallelization strategy for distributed training jobs. In: Proc. 20th USENIX Symp. NSDI; 2023.

[10] Ballani H, Costa P, Behrendt R, et al. Sirius: a flat datacenter network with nanosecond optical switching. In: Proc. ACM SIGCOMM; 2020.

[11] Raja AS, Lange S, Karpov M, et al. Ultrafast optical circuit switching for data centers using integrated soliton microcombs. Nat Commun 2021;12:5867.

[12] Xue X, Calabretta N. Optical switching data center networks: understanding techniques and challenges. arXiv:2302.05298; 2023.

[13] 李沛杰, 等. 面向软件定义晶上系统的安全互连接口架构. 通信学报 2024.

[14] Patarasuk P, Yuan X. Bandwidth optimal all-reduce algorithms for clusters of workstations. J Parallel Distrib Comput 2009;69(2):117–124.

[15] Thakur R, Rabenseifner R, Gropp W. Optimization of collective communication operations in MPICH. Int J High Perform Comput Appl 2005;19(1):49–66.

[16] Amdahl GM. Validity of the single processor approach to achieving large scale computing capabilities. In: Proc. AFIPS Spring Joint Computer Conf.; 1967. p. 483–485.

[17] Esmaeilzadeh H, Blem E, St. Amant R, et al. Dark silicon and the end of multicore scaling. In: Proc. 38th ISCA; 2011.

[18] Shoeybi M, Patwary M, Puri R, et al. Megatron-LM: training multi-billion parameter language models using model parallelism. arXiv:1909.08053; 2019.